# Build State — Akshaya Dental

> **This is the living file.** Every session updates it before finishing. If it disagrees with any other doc, trust this one.

**Last updated:** 2026-08-17 — S12 complete. **The site is build-complete on mock data — all 12 numbered sessions are done.**

---

## ▶ Next session: **After S12 — client-dependent work**

There is no S13. Read `SESSION-PLAN.md` → "After S12" for what's left: swap mock data for real data in `content.js` (should be a single-file edit), real photography, client review + revision rounds, domain/hosting/SSL/GBP/Search Console. All of this depends on the client call — see "Open questions for client" below, which is now the most important section in this file for whoever picks this up next.

---

## Progress

| Session | Status | Shipped |
| :--- | :--- | :--- |
| S1 — Foundation | ✅ Done | Folder structure, `tokens.css`, `base.css`, self-hosted subset fonts (Fraunces/Karla/IBM Plex Mono, 98.6KB total), `content.js`, `_kitchen-sink.html` |
| S2 — The frame | ✅ Done | `components.css` (buttons/pills/chips/header/rail/action bar/WhatsApp float/footer), `main.js` (first `content.js` consumer), header + mobile overlay menu + rail + progress line + sticky action bar + footer added to kitchen sink for verification |
| 🚩 S3 — Hero + Comparator | ✅ Done — awaiting human review | `index.html` (skeleton + hero), `sections.css` (hero, comparator, load-sequence, scroll-reveal), `comparator.js`, `motion.js`, `.visually-hidden` utility added to `base.css` |
| S4 — Homepage part A | ✅ Done — superseded by S4b | Rating monument, treatments grid (10 cards, laser-first, generated from `content.js`), "Why laser" `drape` section with ambient glow |
| S4b — Rebalance rework | ✅ Done | Hero headline → two pillars; two-pillar section added; `gum-treatment` added + `treatments[]` reordered pillar-then-demand (10→11 cards); "Why laser" → "How we work — laser" |
| S5 — Homepage part B | ✅ Done | Meet the dentists, Technology & sterilisation, Patient stories, Visiting the clinic, Questions (FAQ), Closing CTA band. Homepage is now complete. |
| S6 — Treatment template + confirmed | ✅ Done | `assets/css/pages.css`, treatment page template, step-process component, cost block (flag-hidden), 6 live treatment pages (dental-implants, gum-treatment, laser-gingivoplasty, laser-dentistry, root-canal, laser-rct) |
| S7 — Remaining treatments + hub | ✅ Done | 5 remaining treatment pages (crowns-and-bridges, dental-restoration, braces-and-aligners, teeth-whitening, kids-dentistry), `treatments/index.html` hub grouped by pillar, "also offered" list, new "Related treatments" section added across all 11 treatment pages |
| S8 — About, Team, Technology | ✅ Done | `about.html` (story, philosophy, facility, full sterilisation list), `team.html` (full doctor grid) + 3 individual doctor pages under `team/`, `technology.html` (equipment grid + sterilisation workflow as step-process), "Technology" nav link added site-wide |
| S9 — Patient info, Reviews, Contact, Privacy | ✅ Done | `patient-information.html`, `reviews.html`, `contact.html` (booking form + `form.js`), `privacy-policy.html`, new `patientInfo` export |
| S10 — Blog | ✅ Done | `blog/index.html` (5-card grid), post template with sticky rail table-of-contents, 5 seed posts (bleeding gums, root canal, implants vs. bridges, kids' first visit, laser dentistry) |
| S11 — Technical layer | ✅ Done | JSON-LD schema (`Dentist`/`BreadcrumbList`/`FAQPage`/`MedicalProcedure`/`Physician`/`Article`) injected site-wide via new `schema.js`; canonical/OG/Twitter tags + `index.html`'s missing meta description added to all 29 real pages; GA4 conversion tracking (`analytics.js`, gated on a real measurement ID); `form.js` wired to Netlify Forms with a WhatsApp-prefill fallback on the confirmation panel; `favicon.svg`/`.ico`/PNG set + `site.webmanifest`; `robots.txt`; `sitemap.xml` (29 URLs) |
| S12 — QA & polish | ✅ Done | Full Playwright sweep of all 29 pages (0 console errors, 0 failed requests, 0 horizontal overflow at 360/390/768/1024/1440, 0 dead internal links). Fixed 3 real bugs: heading-order skips (H1→H3) on `team.html`/`treatments/index.html`/`blog/index.html`, the `.mobile-menu[hidden]` no-op bug trapping keyboard focus in an invisible off-canvas menu on every page, and the rating monument's SVG arc rendering as a full circle (not 4.9-of-5) under reduced motion. Fixed 2 tap targets below the 48×48 floor (comparator toggle, footer social icons) and the flagged `.booking-form__error` submit-slot spacing on `contact.html`. Verified favicon contrast (6.43:1) and LCP (<1.9s under throttled network) are both fine as-is. Deleted `_kitchen-sink.html`. |

🚩 = stop for human review before continuing.

---

## Carried forward

*Things the next session must know. Out-of-scope issues spotted mid-session go here rather than into the diff.*

- **S4b (2026-08-16) resolved the rebalance** flagged after S4 — `treatments[]` is now ordered pillar-then-demand (`dental-implants` 1, `gum-treatment` 2, `laser-gingivoplasty` 3, `laser-dentistry` 4, `root-canal` 5, `laser-rct` 6, then the 5 non-laser treatments 7–11), not laser-first. Any future session reading `order` should treat this as current — the older "already laser-first" framing no longer applies.
- **`dental-implants.isLaser` is now `true`**, changed in S4b per DESIGN-SYSTEM §1 ("both pillars... delivered with laser"). This is itself `MOCK:`-adjacent — the summary text flags "laser-assisted technique" as unconfirmed protocol detail to verify with the client. If the client says implant placement is *not* laser-delivered, flip this back to `false` and update the summary — nothing else references it structurally (the treatments-grid beam mark and pillar panels both read this field live).
- **All 11 treatments now carry `confirmed: true`** (was `false` on 5 of them pre-S4b) — CONTENT-DATA.md §1's verified service list now covers all eleven, so the earlier "assumed" flags were stale. The `confirmed` field itself isn't rendered anywhere yet (no UI reads it) — it's bookkeeping for the eventual real-data swap.
- **Doctor count stays 3–4 by human decision (2026-08-16)**, despite verification pointing to a single practitioner. Dr. Sampath Kumar Rao K is the verified principal and gets the `--principal` card variant; associates are `MOCK:`. A grid that degrades to one card is cheaper than a single-profile layout that has to grow into a grid.
- `doctors[]` treatment-slug arrays were **not** updated in S4b to reference the new `gum-treatment` slug (Dr. Vikram Shetty's entry still only lists `laser-gingivoplasty`, not `gum-treatment`) — out of S4b's scope (doctors are S5's "Meet the dentists"). Worth adding `gum-treatment` to his `treatments` array when S5 builds the doctor cards, since periodontics is his stated MDS specialism.
- **Never render a years-of-experience number** — sources conflict (23+ vs 15). See CONVENTIONS §1.

- Reduced-motion is handled in `base.css` by zeroing the `--duration-*` tokens inside the media query (no `!important`, per CONVENTIONS.md §4 CSS discipline). Component/section CSS should keep reading durations from those tokens rather than hardcoding transition timings, or this override won't reach them.
- Fraunces was instanced down to two variable axes (`SOFT`, `wght` 400–600) with `opsz` pinned at 48 and `WONK` pinned at 0, rather than shipping all four axes — smaller file, and the design system only specifies fixed SOFT/WONK values plus a wght range anyway. `SOFT` is fixed at 60 via `font-variation-settings` in `base.css` h1/h2 rules. Karla was instanced to a single variable file covering wght 400–700 (one file, not three statics).
- **Any component with a `--drape`/`--drape-deep` background must also carry `data-ground="drape"` on the same element**, or `--fg`/`--fg-muted` stay at their light-ground (dark green) values and text renders invisibly dark-on-dark. Hit this bug with the footer and action bar in S2 — both fixed by adding the attribute. `.site-footer` and `.action-bar` set their background directly (not via `--bg`, since the drape ground token block doesn't redefine `--bg`), so the `data-ground` attribute is doing fg-only inversion work — don't skip it when reusing these classes on future pages.
- `main.js` binds via `data-bind`/`data-cta` but the bound elements ship with **empty** text content and `href="#"` in markup, not the real fallback value — CONVENTIONS.md §3 bans hardcoding client data into markup even as a fallback, since it breaks the "swap content.js and nothing else" guarantee. Every future page's header/footer copy-paste should follow this — empty `data-bind` targets, not pre-filled ones.
- `.rail`'s current-section label needs a `data-rail-label="..."` attribute on each major section for the `IntersectionObserver` in `main.js` to track; homepage sections in S3+ must carry one each or the rail label will just freeze at whatever it last saw.
- Header transparency (`body[data-header-mode="transparent"]`) only applies when the `<body>` tag carries that attribute — set it only on pages with a hero the header can float over (homepage). Inner pages should omit the attribute so the header is solid from load.
- `treatments/index.html`, `about.html`, `team.html`, etc. don't exist yet, so all nav/footer links in the frame markup point at paths that 404 until S6–S10 build them. Expected, not a bug.
- Pre-existing, out of S2's scope: the kitchen sink's S1 "type weights" demo row (`Fraunces 400 500 600` in one `ks-type-row`) overflows horizontally at 360px — a scratch-page-only cosmetic issue, not present on any real component. Leave it; the whole file is deleted in S12.
- `index.html` deliberately omits `data-header-mode="transparent"` — see Decision log. Any future page keeps the header solid unless a real dark/image hero is introduced.
- `.hero`'s bottom padding includes `+ var(--action-bar-height)` below 768px so the section's own end clears the fixed action bar — every future section that can end a page (i.e. is the last section before the footer) on a page with the action bar needs the same clearance, or its bottom content sits under the bar. This is currently handled per-section (mobile-first base + `min-width: 768px` override back to normal), not globally — worth revisiting as a shared utility if it gets repetitive across S4+.
- The WhatsApp float (≥768px) and the sticky action bar (<768px) are fixed-position and will overlap whatever content is currently scrolled beneath them — confirmed this is expected/standard floating-button behavior, not a layout bug, after seeing it in the S3 screenshots.
- Comparator row copy ("Drill used / Sutures / Typical sittings / Anaesthesia", values incl. "Often none") is copied directly from DESIGN-SYSTEM.md §6's own worked example — treated as pre-vetted DCI-compliant copy, not `MOCK:`-flagged, since it states generic laser-vs-conventional procedure attributes rather than an Akshaya-specific fact. `CONTENT-DATA.md` doesn't model comparator rows in `content.js`; they're hardcoded in `index.html`/`comparator.js` on the reasoning that "never hardcode client data" (CONVENTIONS §3) covers name/phone/address/doctor/timing facts, not this kind of system copy shared by any laser-dentistry clinic. Flag if a future session disagrees.
- `motion.js` must load after `main.js` in `index.html`'s script order — it reads and re-splits the hero heading's already-bound text into `.load-word` spans for the stagger effect, so it depends on `main.js`'s `data-bind` pass having already run. Module scripts execute in document order, so keep `main.js` → `comparator.js` → `motion.js` on every future page that includes the hero heading pattern.
- **CONVENTIONS.md §5 vs DESIGN-SYSTEM.md §6 conflict, resolved in favour of CONVENTIONS:** the component inventory describes the treatment card as carrying a "Plex Mono index," but CONVENTIONS.md §5 states numbering (01/02/03) is allowed in exactly one component — the treatment step process — nowhere else. S4 built the treatment card **without** an index number, using only the amber beam mark (laser treatments) + title + description + sittings chip + arrow. Follow this in S6/S7 when building the step process and reusing/extending the card — the step process is the only place `01→05` numbering may appear.
- Treatment cards are generated entirely in `main.js` (`#treatmentsGrid`), sorted by `content.js`'s `order` field, which is already laser-first (orders 1–3 are the laser treatments). Per-card scroll-reveal stagger is set as an inline `transition-delay` in the generated markup (same reasoning as `motion.js`'s hero word-stagger: card count is data-driven, so a fixed CSS `:nth-child` chain doesn't fit). Static (non-generated) multi-item reveal groups — like `.why-laser__grid`'s 4 items — use CSS `:nth-child` stagger instead; see the rule pattern in `sections.css` if a future session adds another static grid that needs staggered reveal.
- Added `--duration-ambient: 20s` to `tokens.css` for the "Why laser" section's ambient amber glow drift (DESIGN-SYSTEM §8 specifies 20s). Any future `drape` section that wants the same ambient glow effect should reuse this token, not introduce a new duration literal.
- The rating monument's arc fill uses a hardcoded `stroke-dashoffset: 0.02` for 4.9-of-5 stars (calculated as `1 - 4.9/5`). This is **not** wired to `clinic.rating` dynamically — if the real rating ever changes from 4.9, this value in `sections.css` (`.rating-monument.is-revealed .rating-monument__arc-fill`) needs a manual update, or a future session should make it computed (e.g. a CSS custom property set inline from `main.js` reading `clinic.rating`). Flagged here since `content.js`-only edits are supposed to be sufficient for data changes (CONVENTIONS §3) and this is a small exception.
- Confirmed via Playwright: `fullPage: true` screenshots can visually corrupt/duplicate `position: fixed` elements (saw the mobile-menu's logo/hamburger bleed into the middle of a full-page capture at 360px even though it's `hidden` + `opacity: 0`). This is a Chromium/Playwright full-page-screenshot stitching artifact, not a real rendering bug — a normal single-viewport screenshot at the same scroll position renders correctly. Don't chase this if it recurs in future sessions' QA; verify with a viewport-clipped screenshot instead of `fullPage` when a fixed-position element looks wrong.
- **S5 found and fixed a real gap from S1**: `content.js`'s `doctors[]` was entirely mock — it never included the verified real principal, Dr. Sampath Kumar Rao K, despite `CONTENT-DATA.md` §1/§4 clearly specifying him as the principal with an `isPrincipal`/`practisingSince` shape. S5 rewrote `doctors[]` to open with the real Dr. Sampath (`isPrincipal: true`) followed by 2 MOCK associates (3 total, matching the S1 decision-log entry "mock doctor count fixed at 3"). The old 3rd mock associate, "Dr. Vikram Shetty" (implants + laser-gingivoplasty), was dropped rather than kept — Dr. Sampath's real specialisation already covers implants + gum-treatment + laser, so a third mock doctor covering the same ground was redundant. This also resolves the older carried-forward note about adding `gum-treatment` to Vikram's `treatments[]` array — moot now that he's gone. If a future client call reveals more real associates, add them as their own MOCK-flagged (or, once confirmed, real) entries rather than reviving Vikram.
- **`doctors[].experienceYears` (a raw number) was replaced with `practisingSince: null`** for every doctor, matching `CONTENT-DATA.md` §4's canonical shape. It is intentionally left `null`, not a guessed MOCK year — CONVENTIONS §1 bans rendering a years-of-experience figure outright, and the two public sources for Dr. Sampath disagree (23+ vs 15 years). Nothing in S5 reads or renders this field. Once the client confirms a start year, a future session can render "in practice since &lt;year&gt;" on the doctor card/credential block.
- Added `equipment[]` and `sterilisation[]` (both fully `MOCK:`-flagged) as new top-level `content.js` exports — not in `CONTENT-DATA.md`'s original shape sketch, but needed for the homepage Technology & sterilisation section and structurally identical to how `treatments[]`/`doctors[]` already work (array of objects, single source, `MOCK:`-flagged). `CONTENT-DATA.md` §4 doesn't need updating for this — its shape sketch was always "not a limit," per `00-START-HERE.md`. S8 (the full Technology page) should reuse these same two exports rather than inventing a second data shape.
- **Resolved a real conflict inside `DESIGN-SYSTEM.md` itself**: §3's Restraint rule states Fraunces is used only at `h1`/`h2`/pull-quotes/rating monument, "including `h3`" is explicitly Karla — but §6's component inventory describes the doctor card's name as "in Fraunces." S5 followed the Restraint rule (doctor names render as Karla `h3`, same pattern as `.treatment-card__title`), treating the more fundamental, explicitly-stated design rule as authoritative over the shorthand component-inventory description — same resolution logic S4 used for CONVENTIONS vs. the component inventory on step-process numbering. Flag if a future session/human disagrees.
- The homepage's "Patient stories" section renders `content.js`'s `reviews[]` as static cards — there is no live Google Reviews embed wired into this static build (no API key/backend exists yet). Per S5's scope ("reviews embed, with a static fallback if the embed can't load"), the static cards **are** the fallback; wiring an actual live embed is unscoped future work (S9/S11 territory, needs a Google Places API key or a third-party widget script — the latter would be the first external script dependency in a build that's otherwise zero-dependency, so it needs a conscious decision, not a default add).
- The "Visiting the clinic" section's map is a `placeholder-block` (bisque, dashed border, Plex Mono label), same pattern as doctor/equipment photos — `clinic.address.geo` is still `{ lat: null, lng: null }` and `mapsUrl` is still `MOCK: —`, so there's no real map to embed yet (Open question #9). The "Get directions" button (`data-cta="directions"`) is wired via the existing `main.js` logic from S2 but will stay a dead `href="#"` until `mapsUrl` is real.
- Added a new shared `.placeholder-block` utility class to `components.css` (flat bisque block, dashed `--drape-mid` border) for the doctor-photo/equipment-photo/map placeholders — reuse this for any future placeholder image (blog cards, facility photos in S8/S10) instead of rewriting the same three declarations per component.
- `clinic.parking` is `"MOCK: —"` and now renders literally as visible text in the "Visiting the clinic" section (previously no template rendered this field). Same treatment as the address `landmark`/`mapsUrl` placeholders — an honest "unknown" marker rather than an invented guess, since there's no plausible default for parking availability. Swap for real copy once confirmed (Open question — add parking specifics to the client call if not already covered).

- **S6 built the treatment page template as a static-HTML generator, not a runtime template.** All 6 `treatments/*.html` files were produced from a one-off Python script (`gen_treatment_pages.py`, not shipped — deleted after the run) that interpolates only the slug and the `<title>`/`<meta description>` per page; every other piece of treatment-specific content (name, lead, sittings, duration, signs, steps, aftercare, FAQs, cost range) is populated at runtime by a new block in `main.js` (section 16, "treatment page template") reading `content.js`'s matching `treatments[]` entry via `document.body.dataset.treatmentSlug`. **S7 should reuse this same generator-script pattern** for the remaining 5 pages rather than hand-writing HTML — copy `gen_treatment_pages.py`'s structure (it's gone now, but any S6 `treatments/*.html` file is the literal template to lift from) and add the 5 new `PAGES` entries with `has_comparator: False` for all of them (CONTENT-DATA §5: none of rows 7–11 use the comparator).
- **`<title>`/`<meta description>` on the 6 new treatment pages are hand-written per file, not data-bound.** This is a deliberate, scoped exception to CONVENTIONS §3's hardcoding ban: `<title>` must be present in static HTML for SEO/social-preview crawlers that don't execute JS, so it can't be JS-populated the way body content is. SESSION-PLAN.md's S11 ("Per-page unique `<title>` + meta description + canonical + OG/Twitter tags") explicitly owns systematizing this later — if a treatment's `name` in `content.js` ever changes, its `<title>` needs a matching manual edit until S11 exists. Flagged here so S11 doesn't miss these 6 (and later 5) files.
- **Treatment pages use root-relative asset/nav paths** (`/assets/css/...`, `/index.html`, `/treatments/index.html`, etc.), not page-relative (`../assets/...`). This differs from `index.html`'s existing page-relative links (`treatments/index.html`, `contact.html`) — chosen because `main.js`'s footer generator already emits root-relative treatment links (`/treatments/${t.slug}.html`, since S2), and root-relative paths don't need adjusting as page depth grows (e.g. future blog posts under `blog/`). `index.html` itself was left untouched (out of S6's scope) — its relative links still work fine from the root, so this is not a bug, just an inconsistency between the homepage and every inner page. Worth a deliberate pass to unify one way or the other before launch, not urgent.
- **Comparator rows are identical across all 5 pages that embed one** (dental-implants, gum-treatment, laser-gingivoplasty, laser-dentistry, laser-rct) — same four rows/values as the homepage's comparator (Drill used, Sutures, Typical sittings, Anaesthesia), not adapted per-procedure. SESSION-PLAN.md's S6 scope line says rows should be "adapted to that procedure," but inventing new procedure-specific attribute claims without a source risks a DCI compliance problem (CONVENTIONS §1's does-vs-gets test still passes, but the specific numbers would be unverified guesses). Reusing the already-vetted generic rows was judged the safer call. See Decision log — revisit if the client call surfaces real procedure-specific data.
- **`.sterilisation-list`/`.sterilisation-list__item`/`__mark` classes (built in S5 for the homepage sterilisation protocol) are reused as-is for every treatment page's aftercare list.** It's a generic checkmark list with no sterilisation-specific styling — reusing it avoided a near-duplicate CSS block. The class names are now slightly misleading outside their original context; a future session could rename to something generic like `.check-list` in one pass across both usages if it bothers a future maintainer. Not urgent.
- **`content.js`'s `costRange` shape landed as `{ min, max, unit, emi }`** (e.g. `{ min: 25000, max: 45000, unit: "per implant", emi: true }`) for the 6 S6 treatments — CONTENT-DATA.md §4's shape sketch only showed `costRange: null` with no object shape, so this is a new addition, not a limit violation. `laser-dentistry.costRange` is deliberately `null` (not an object) since it's a method hub, not a billable procedure of its own — its cost block section ships with the same hidden/flag-gated markup as the others but never gets populated even if `showPricing` flips true, since `main.js`'s populate step is guarded by `t.costRange` truthiness. S7 should follow the same `{min,max,unit,emi}` shape for the remaining 5 treatments' cost ranges.
- The step-process component's connecting beam line (the visual joining 01→05) is a CSS `::after` pseudo-element on every item but the last, absolutely positioned — vertical on mobile (a left-aligned line down through stacked items), horizontal on desktop (`min-width: 1024px`, a top-aligned line across the row). This is the only component site-wide using 01/02/03-style numbering, per CONVENTIONS §5 — don't reuse `.step-process__index` styling elsewhere.
- Verified via Playwright that the known `fullPage: true` screenshot artifact (documented in an earlier Carried Forward entry) reproduces on the treatment pages too — a `fullPage` capture with `prefers-reduced-motion: no-preference` (the default) shows most `[data-reveal]` sections as blank space, because Chromium's full-page stitching doesn't fire `IntersectionObserver` the way a real incremental scroll does. Confirmed this is not a real bug two ways: (1) forcing `prefers-reduced-motion: reduce` before navigating shows full content immediately (reduced-motion skips the observer and adds `.is-revealed` synchronously), and (2) a real incremental `scrollTo` loop followed by a `fullPage` capture shows all 39 `[data-reveal]` elements correctly revealed. Use one of those two methods for future full-page QA screenshots on any page with scroll reveals, not a bare `fullPage` capture.

- **S7 added a new shared helper, `treatmentCardHtml(t, hrefPrefix, delay)`, in `main.js`** (module scope, near the homepage treatments-grid generator) so treatment-card markup has exactly one source instead of drifting between the homepage grid and the new hub grid. It takes an `hrefPrefix` argument (`"treatments/"` from the homepage, `"/treatments/"` from the hub, since the hub lives one directory deeper) — any future third place that renders treatment cards should call this helper too, not re-copy the markup string.
- **The treatments hub's pillar grouping (`Implants` / `Gum & periodontal care` / `Root canal` / `The laser method` / `General & family dentistry`) is hand-written page structure in `main.js`'s new hub generator, not a `content.js` field.** CONTENT-DATA.md §4 has no pillar model, same reasoning S4b used for the homepage's two-pillar panels. If a future session wants pillar membership to be data-driven (e.g. so About/Team pages can also group by pillar), add a `pillar` field to each `treatments[]` entry rather than duplicating this grouping logic a third time.
- **Added a new "Related treatments" section to the shared treatment page template**, rendered from each treatment's existing `related[]` field (present in `content.js` since S1, previously unrendered anywhere). This touched all 11 treatment pages, including the 6 S6 already shipped — done deliberately, not scope creep, since SESSION-PLAN.md's S7 scope explicitly names "related-treatment links between pages." Placed between the credential block and the CTA band. Any future new treatment page must include this section too, or its `related[]` data silently goes unused.
- **Added `alsoOffered` as a new top-level `content.js` export** (6 MOCK-flagged strings: dentures, veneers/laminates, wisdom tooth extraction, scaling & polishing, tooth extraction, minor oral surgery) — CONTENT-DATA.md §5's "also offered" list existed only as prose in that doc, not in `content.js`. Followed the same precedent as S5's `equipment[]`/`sterilisation[]` additions: new export, not a second shape. Rendered on the hub page via `.chip`-styled `<li>`s, reusing the existing `.chip` component rather than writing new list-item CSS.
- **S8 added `about` and `facility` as new top-level `content.js` exports** (both `MOCK:`-flagged) — `about.story`/`about.philosophy` are prose paragraphs for the About page, `facility` is 4 labelled photo placeholders (3:2, per CONTENT-DATA §7). Same precedent as S5's `equipment[]`/`sterilisation[]` and this session's `alsoOffered` neighbour: new export, not a second shape. Not yet named in CONTENT-DATA.md §3's "fully invented" list, but covered by its general "clinic story and founding details" line.
- **Individual doctor pages live at `team/<slug>.html`, one file per `doctors[]` entry**, generated the same one-off-script-then-delete way S6/S7 generated treatment pages (script wasn't kept). Each carries `data-doctor-slug` on `<body>`, mirroring the `data-treatment-slug` pattern exactly (new `activeDoctor` derived value + `data-bind-doctor` attributes + a `main.js` section 20 template block). **Caveat:** two of the three doctor slugs are `MOCK-dr-anitha-rao` / `MOCK-dr-farah-khan` (the `MOCK-` prefix is literally in the slug, inherited from `content.js` since S5) — those filenames will need renaming alongside the `content.js` edit when real associate data (or a real doctor count) arrives, since static per-doctor pages can't rename themselves the way `content.js`-only edits can. Flagged here so the real-data swap doesn't miss it; not a bug in this session.
- **Technology page's sterilisation section reuses the `.step-process` component CSS as-is** (same classes as the treatment-page step process, CONVENTIONS §5's one numbering exception) to present `sterilisation[]`'s 4 strings as an ordered workflow, while About page's "full sterilisation protocol" section reuses `.sterilisation-list` (the checklist component from S5) for the same 4 strings. One array, two presentations, no new data shape — see Decision log.
- Doctor pages' "treatments performed" list reuses `.related-treatments__link` styling (from S7) via a new `#doctorTreatmentsList` generator — same visual pattern (pill-style link with an arrow), different data source (`doctors[].treatments[]` instead of `treatments[].related[]`).
- **Added a site-wide "Technology" nav link** (desktop nav, mobile overlay nav, footer "Site" block) to all 13 pre-existing pages (`index.html` + 11 treatment pages + the treatments hub), not just the 3 new S8 pages — `technology.html` would otherwise have had zero inbound links from anywhere but `about.html`/`team.html`, an orphan page. Done mechanically via a scripted insert matching each file's existing path convention (page-relative on `index.html`, root-relative `/technology.html` elsewhere — see the pre-existing S6 Carried-forward note on that inconsistency, left untouched here). QA'd via Playwright that both `index.html` and a treatment page still render with zero console errors/failed requests/overflow after the edit.
- **All 5 new treatments' `costRange` values are freshly invented MOCK figures**, not yet flagged against Open question #13 in this file (that question currently only names the 5 S6 treatments). Worth adding the 5 S7 slugs to Open question #13's scope, or opening a matching line — flagged here so a future session doesn't miss confirming pricing for crowns-and-bridges/dental-restoration/braces-and-aligners/teeth-whitening/kids-dentistry alongside the original five.
- QA'd via a Playwright agent across all 7 touched/new pages (the hub + 5 new treatment pages + `dental-implants.html` as a spot-check that the related-treatments addition didn't regress an S6 page) at 360×800/768×1024/1440×900: zero console errors, zero failed requests, zero horizontal overflow, hub grid renders exactly 11 cards in 5 groups, also-offered list renders 6 items, every new page's signs/steps/aftercare/FAQ/related lists populate from data, cost blocks stay `hidden` (showPricing still false), and scroll-reveal correctly takes all 29 `[data-reveal]` elements on the hub page from 5→29 `.is-revealed` over an incremental scroll.

- **S9 added a new top-level `content.js` export, `patientInfo`** (`firstVisit[]`, `whatToBring[]`, `payment[]`, `emiNote`, `insurance`, `emergency` — all `MOCK:`-flagged), following the same precedent as `equipment[]`/`sterilisation[]`/`about`/`facility`: a new export, not a second data shape. Rendered on `patient-information.html` via the reused `.sterilisation-list` checkmark-list component (same class reused a third time now, across sterilisation protocol / treatment aftercare / patient-info lists — still just a generic checkmark list, no protocol-specific styling).
- **The booking form (`contact.html`) has no real backend yet.** `form.js` (new file, per CONVENTIONS §2's file list) does full inline validation (name, phone pattern, treatment-select, date, consent checkbox) but on a valid submit just hides `#bookingForm` and reveals a static `#bookingConfirmation` panel — nothing is actually sent anywhere. SESSION-PLAN.md's S11 explicitly owns "Form handler (Netlify Forms or Formspree) + WhatsApp notification path"; S9 stayed in scope by building the component fully testable ahead of that wiring, same reasoning S5 used for the reviews section's static fallback. **S11 must wire real submission into `form.js`'s existing valid-submit branch**, not replace the validation logic.
- `#bookingTreatment`'s `<option>` list is generated in `main.js` (section 23) from `treatments[]` sorted by `order`, plus a hand-written "Not sure yet" option appended last — not a `content.js` field, same reasoning as the treatments-hub pillar grouping (S7) for why small pieces of page structure don't need to become data.
- Fixed a real bug caught by Playwright QA during this session: `.booking-form__error:empty { display: none; }` (now removed from `pages.css`) caused the reserved `min-height: 1.2em` error-message slot to collapse when empty, which shifted the consent checkbox by ~30px whenever a sibling field's error text appeared/cleared — a mouse click landing mid-shift could miss the checkbox (mousedown fired, mouseup/click/change did not). The error slot now always reserves its `min-height` whether or not it has text, so nothing shifts. Also fixed: `.booking-form` had no `[hidden]` rule, so `form.js` setting `form.hidden = true` on successful submit set the DOM attribute but the element's own `display: flex` declaration kept it visually on screen, overlapping the confirmation panel. Added `.booking-form[hidden] { display: none; }`, mirroring the rule `.booking-confirmation[hidden]` already had. **Any future new component that gets JS-toggled via the `hidden` attribute needs its own `[hidden] { display: none; }` rule** — author CSS on the base selector always wins the specificity tie against the UA's default `[hidden]` rule, so `hidden` alone doesn't reliably hide anything with its own `display` declaration.
- `clinic.email` is still `"MOCK: —"` and now renders literally as visible text on `contact.html` and `privacy-policy.html` (previously unused anywhere) — same honest-placeholder treatment as `parking`/`mapsUrl` since S5. Needs a real address before launch (Open question #8, already tracked).
- `privacy-policy.html` is written as static prose (data-bound only for `email`/`phoneDisplay`/`name`/`locality`/`city`), not modeled in `content.js` — same reasoning S3 used for the comparator's row copy: this is generic DPDP-notice legal language, not Akshaya-specific client data, so CONVENTIONS §3's hardcoding ban doesn't apply to it the way it does to name/phone/address facts. The effective date (16 August 2026) is hand-written in the page and will need a manual bump if the policy text changes before launch.

- **S10 added a new top-level `content.js` export, `blogPosts`** (5 fully-`MOCK:`-flagged seed posts, per CONTENT-DATA §3's explicit "blog posts" listing under "fully invented — replace before launch"). Followed the doctor-associate precedent (not the treatment-name precedent) for the `MOCK:` prefix placement: since the whole post is invented, `title`/`excerpt`/`coverLabel` and every `body[].paragraphs[]` string carry the literal prefix and render it as-is in the live build — same as `doctors[]`'s `"MOCK: Dr. Anitha Rao"` showing verbatim on doctor cards. `<title>`/`<meta description>` on the 5 HTML files are hand-written and clean (no MOCK prefix), same S6 precedent, since they're SEO-facing and not rendered from `content.js` at all.
- **Each post's `body` is `[{id, heading, paragraphs[]}]`** — this single array drives three things from one source: the post's rendered content (`main.js` section 25 → `#postBody`), the rail's table of contents (`#postToc`, same section), and the anchor IDs the TOC links jump to. Adding/reordering/renaming a post's sections only ever touches `content.js`.
- **The blog post's rail is a genuinely different rail mode from every other page**, per DESIGN-SYSTEM §7's explicit call for "a sticky table of contents in the rail on desktop." Instead of the standard `.rail__label` (single current-section text, updated by the existing section-6 `IntersectionObserver` in `main.js`) + `.rail__track`/`.rail__fill` scroll-percentage bar, post pages ship `<nav class="rail__toc">` — a static list of anchor links, one per `body[]` entry, with a **second, independent** `IntersectionObserver` (`main.js` section 25, `rootMargin: "-20% 0px -70% 0px"`) toggling `.is-active` on whichever link's target heading is in the "reading zone." This observer is scoped entirely to post pages (only instantiated when `#postToc` exists) and doesn't touch or conflict with the shared rail-label-tracking or scroll-reveal observers already running on every page. `.rail__toc`/`.rail__toc-link` live in `components.css` beside `.rail__label`/`.rail__track` (not `pages.css`) since they're a variant of the shared rail component, not page-specific content — matches the CONVENTIONS §4 layer-order reasoning. The mobile "TOC hidden" requirement needed no extra code: `.rail` itself is already `display: none` below 1024px, so a TOC-mode rail inherits that for free.
- **Post hero and card cover images are 16:9** per CONTENT-DATA §7's "blog cards 16:9" line — applied to both the index card photo and the individual post's hero cover placeholder, for a consistent aspect ratio if/when a post's cover photo is later promoted to the index card thumbnail.
- **The 5 posts were authored individually as separate HTML files** (not a one-off-script-then-delete like the treatment/doctor pages) since each post's prose is fully bespoke, not a shared template with only a few interpolated fields — the "template" here is the file structure (hero/body/CTA-band shell), which was written once and copied, with `<title>`/`<meta description>`/`data-post-slug` hand-edited per file. This is a smaller version of the same S6 precedent (author markup once, let `content.js` drive the variable parts), just without a disposable script since there was no repetitive per-field interpolation to script.

- **S11 added `clinic.siteUrl`** (`https://www.akshayadentalclinic.in`, a plausible but unregistered `.in` domain — Open question #12) to `content.js`. Unlike every other invented field, this string is **not** `MOCK:`-prefixed — it's consumed directly as a URL by `schema.js` (JSON-LD `@id`/`url` fields) and by the one-off `gen_meta_tags.py`/`gen_script_tags.py` scripts that stamped canonical/OG/Twitter tags and the favicon links into all 29 pages' static `<head>`s (both scripts deleted after running, per the S6+ generator-script precedent). **If the real domain changes, both the `content.js` constant and every static `<link rel="canonical">`/`og:url`/`twitter:*` tag need updating** — re-running an equivalent script is faster than hand-editing 29 files. `robots.txt` and `sitemap.xml` also hardcode this same string and need the same swap.
- **`schema.js` reads the existing `.breadcrumb__list` DOM (built by S6–S10) to generate `BreadcrumbList` JSON-LD**, rather than a second breadcrumb data model — every inner page already carries this markup, so this was a zero-cost reuse. It runs after `main.js` in script order specifically so dynamic breadcrumb labels (`data-bind-treatment="name"` etc.) are already populated before being read back out. Similarly, `FAQPage` schema is built by querying `.faq-item` elements generically (works for both the homepage accordion and any treatment page's), not by re-reading `faqs`/`t.faqs` from `content.js` a second time.
- **Schema content deliberately mirrors visible page text exactly, `MOCK:` prefixes included** (e.g. a treatment's `MedicalProcedure.description` will read `"MOCK: Titanium implant..."` verbatim) — stripping the prefix only in structured data while it stays visible in the rendered page would create a mismatch between what a crawler reads and what a visitor sees, which reads as cloaking. This means Google's Rich Results Test will flag/show `MOCK:`-prefixed content until the real-data swap — expected at this build stage, not a bug. The swap is still single-file: `schema.js` reads `content.js` directly, so replacing mock values there fixes both the visible page and the schema at once.
- **No `image` field on the `Dentist` schema, no `og:image`/`twitter:image` on any page.** No real photography or logo exists yet (every photo on the site is a labelled `.placeholder-block`, not an actual image file — Open questions #10/#11), so there's no real asset to reference and a broken image URL in structured data / social previews is worse than omitting the field. **S12 or a later session must add these once real photography/logo lands** — Rich Results and social-share previews both want an image.
- **GA4 (`analytics.js`) is fully wired but inert until a real property ID lands** — `content.js`'s `analytics.ga4MeasurementId` is `"MOCK: G-XXXXXXXXXX"`, and `analytics.js` only loads `gtag.js`/fires events when the ID matches `/^G-/`, so the placeholder never makes a network call. All five conversion events are hooked: `call_click`/`whatsapp_click`/`directions_click` via existing `[data-cta]` click listeners (no markup changes needed — same elements every page already had), `comparator_toggle` via a new `comparator:toggle` `CustomEvent` `comparator.js` dispatches on a real user toggle only (not the initial mount), `form_submit` via a direct `trackEvent()` import in `form.js`, fired only after a submission actually succeeds. Once the client provides a real GA4 ID, it's a one-line `content.js` edit — nothing else changes.
- **The booking form now has a real handler: Netlify Forms.** `contact.html`'s `<form>` gained `name="booking"`, `data-netlify="true"`, `netlify-honeypot="bot-field"`, a hidden `form-name` input, and a visually-hidden honeypot field — the static markup Netlify's build-time form detection needs. `form.js`'s valid-submit branch now does an async `fetch("/", { method: "POST", ... })` with the form's own fields URL-encoded, matching Netlify's documented AJAX-submission pattern. **This only works once the site is actually deployed on Netlify** (Open question #12/Decision log — hosting choice per PROJECT-BRIEF §6). Tested locally against both a POST-rejecting server (correctly shows the new `#bookingSubmitError` message, form stays visible, submit button re-enables) and a POST-accepting mock server (confirmation reveals, WhatsApp link populates).
- **"WhatsApp notification path" (SESSION-PLAN §S11) was built as a client-side fallback, not a server webhook.** A true Netlify Forms → WhatsApp webhook requires a third-party integration (Zapier/Make/Twilio) configured in a dashboard after deployment — not something this session could wire from a static codebase. Instead, `#bookingConfirmation` now shows a second CTA, `#bookingConfirmationWhatsapp`, populated on successful submit with a `wa.me` link pre-filled with the submitted name/phone/treatment/day — so the clinic can be notified immediately over WhatsApp even before (or instead of) a Netlify email notification arrives. Revisit if the client wants a true automated webhook once real hosting exists.
- **Favicon is a generated mark, not the client's real logo** (Open question #10 — logo source file still pending). `favicon.svg` reuses the exact beam-mark motif already in `main.js`'s `treatmentCardHtml()` (a diagonal amber line into a small circle, on the `--drape` ground) rather than inventing a new shape, so it reads as part of the same visual system instead of a generic default. Rasterized to `.ico`/16/32/180/192/512px PNGs via ImageMagick (`convert`), palette-reduced to keep file sizes small (favicon.ico is 5.4KB, the 512px PNG 15KB). **Swap `favicon.svg` for the real logo mark once the client provides one, then re-run the same `convert` commands** to regenerate the raster set; nothing else references the logo's actual appearance.

- **S12 confirmed there is still zero real photography anywhere in the codebase** — `assets/img/{doctors,equipment,facility,blog}/` are all empty directories, and a full-codebase grep found no `<img>` tag at all, only `.placeholder-block` divs. This means the S12 SESSION-PLAN scope item "image conversion to WebP/AVIF, explicit width/height, lazy loading" had **nothing to act on** — there is no raster image to convert, resize, or lazy-load yet. This is not a gap in S12's work; it's correctly deferred until Open question #11 (photography) resolves. **Whoever does the real-photography swap must add `loading="lazy"` + explicit `width`/`height` to every new `<img>` at that time** — nothing currently in the codebase does this because nothing currently needs it.
- **The `.mobile-menu` overlay had no `[hidden] { display: none; }` rule**, the exact same bug class S9 found and fixed on `.booking-form` — an element with its own `display` declaration (`.mobile-menu { display: flex; ... }`) silently defeats the native `hidden` attribute, because author CSS always beats the UA default stylesheet regardless of specificity. Since `.mobile-menu` is `position: fixed; inset: 0` and only visually hidden via `opacity: 0` + `pointer-events: none` (needed for the open/close transition), the bug meant the **closed** overlay was still fully laid out and in the keyboard tab order on every single page, site-wide, since S2. A `Tab` from the header logo went logo → hamburger → the invisible mobile-menu close button, derailing keyboard navigation before it ever reached real page content. Fixed with `.mobile-menu[hidden] { display: none; }` (S12, `components.css`) — `main.js`'s existing `mobileMenu.hidden = !open` toggle already managed the attribute correctly, so no JS change was needed. **Any future new full-viewport overlay component that sets its own `display` and relies on the native `hidden` attribute needs this same paired rule, every time** — this is now the second time this exact bug has shipped from two different components (S9's booking form, S2's mobile menu). Worth a global reminder rule if a third overlay is ever added.
- **The rating monument's SVG arc had no `stroke-dasharray`/`stroke-dashoffset` at all under `prefers-reduced-motion: reduce`** — only the `no-preference` media block set those properties (animating `dashoffset` from 1→0.02 on scroll-reveal), so reduced-motion users saw a plain, undashed circle stroke, which renders as a full 100% ring — visually claiming a 5.0 rating instead of the real 4.9. Fixed by adding a `prefers-reduced-motion: reduce` block that sets the same final `stroke-dasharray: 1; stroke-dashoffset: 0.02;` with no transition, so reduced-motion users see the correct ~98% arc immediately, matching CONVENTIONS §6's "SVG arcs render complete" requirement (complete = correct final state, not literally "full circle"). This affects both `index.html` and `reviews.html` (same component, same CSS, no page-specific fix needed).
- **Two component-scoped tap targets were below the CONVENTIONS §7 48×48px floor**: the comparator toggle buttons (`min-height: 40px`, a raw px literal) and the footer social icons (`width`/`height: 36px`, also raw px literals — both were themselves violations of `tokens.css`-only-raw-literals). Both bumped to `var(--space-12)` (48px), fixing the tap-target floor and the literal-value discipline in the same edit.
- **Not fixed, flagged instead — footer treatment-links list and inline contact links (phone numbers, "privacy policy") are well under 48×48px** (typically ~15px tall, single-line text links in a dense list). This is a site-wide pattern established since S2 across all 29 pages' footers, not a one-page bug — fixing it properly means a deliberate footer-density redesign (larger tap zones per link, which either means fewer links per screenful or more footer height on every page), not a quick CSS value bump. Inline text links (the phone number inside a paragraph) commonly fall under WCAG 2.5.5's own "target is in a sentence" exception, so those specifically may not need a fix at all. Flag for a future design decision, not a QA-session fix — it's the same category of judgment call as S7's "not a build step, but structural" calls, and touching every footer 29 times late in a 12-session build carries real regression risk for a floor CONVENTIONS itself treats as a per-session "verify," not "rebuild."
- **Cross-browser testing was Chromium-only** — this sandbox has no Firefox or WebKit executables available to Playwright (`browserType.launch` fails, binaries not installed). Every check in S12 (console errors, overflow, reduced motion, contrast, tap targets, dead links) was verified in Chromium only. A future session or the human should spot-check Safari/Firefox once real hosting exists, particularly the `<details>`/`<summary>` FAQ accordion animation and the CSS `grid-template-rows` 0fr→1fr trick (both have historically had cross-engine quirks).
- **LCP verified well under the 2.5s budget even under throttled network** — tested `index.html`, a treatment page, and the blog index at 360×640 with CDP `Network.emulateNetworkConditions` (~1.6Mbps down / 750Kbps up / 150ms RTT) plus 4× CPU throttling: LCP landed at 1.3–1.8s across all three. No fonts are preloaded via `<link rel="preload">` (they rely on `font-display: swap` + browser priority alone) — this has headroom to spare given the current LCP margin, so not flagged as a fix, just noted in case a future session wants to shave it further once real images add weight to the page.
- **Favicon legibility/contrast at 16×16 (S11's flagged item) verified fine, no change needed.** Rendered the actual `favicon-16x16.png` at native size and against both light and dark synthetic tab backgrounds — the rounded-square mark reads clearly as a distinct shape. Computed WCAG contrast between `--beam` (#E3A24A) and `--drape` (#12302A) is 6.43:1, well past AA even for large text/graphics. No action taken.
- **The `.booking-form__error` submit-slot placement (S11's other flagged item) had a real spacing bug, now fixed.** `#bookingSubmitError` was a direct flex child of `.booking-form` (24px gap token on every side), so even when empty it reserved a dead ~64px gap between the consent checkbox and the submit button — roughly 2.5× the ~24px rhythm used between every other field. Fixed by wrapping the error paragraph and the submit button in a new `.booking-form__submit` flex group with an 8px inner gap (`var(--space-2)`), mirroring the tighter per-field error-to-input spacing pattern already used elsewhere in the form. Verified both the empty state (now a clean, consistent ~24px gap before the button) and a forced submit-failure state (error text now reads as tightly attached to the button it explains, not floating in the middle of the form) via Playwright screenshots at 390×844.

- **2026-08-17 (post-S12, pre-pitch) — the two fabricated associate doctors were removed by explicit human decision, reversing the 2026-08-16 "keep 3–4 doctors" call.** Rationale for the reversal: this build is about to be shown *to the real Dr. Sampath* to close the sale, and presenting him with two invented colleagues (`MOCK-dr-anitha-rao`, `MOCK-dr-farah-khan`) risks him spotting the fabrication in seconds and auditing the rest of the site for what else is fake — a materially worse outcome than one honest doctor. Changes: `content.js`'s `doctors[]` now holds only the verified principal, Dr. Sampath, with his `treatments[]` array expanded to cover all 11 treatment slugs (previously the two mock associates covered several slugs between them — every treatment page's `performingDoctor` lookup still resolves correctly, verified against the live slug list). The two `team/MOCK-dr-*.html` files were deleted, and their two `sitemap.xml` `<url>` entries removed (27 pages ↔ 27 sitemap URLs, confirmed matching). "Meet the team"/"Meet the dentists" plural copy was changed to "Meet your dentist" across `team.html` (H1, hero lead, hidden H2, rail label, `<title>`/meta description/OG/Twitter tags all left as generic "Our Team" since that phrase doesn't itself imply plural), `index.html` (homepage section eyebrow + rail label), and all 11 treatment pages' credential-block link text ("Meet the team →" → "Meet your dentist →").
- **A fake-looking registration number (`"MOCK-KA-00000"`) was replaced with `registrationNo: null` plus a new `formatRegNo()` helper in `main.js`** (`d.registrationNo ? "Reg. no. ${regNo}" : "Reg. no. on file at clinic"`), applied at all four render sites (homepage doctor grid, team-page doctor grid, treatment-page credential block, individual doctor-page `data-bind-doctor='reg'`). Same reasoning as the doctor removal: a plausible-but-fake council registration number next to a real dentist's real name is a worse failure than an honest "on file at clinic" placeholder. **Any future new doctor-rendering call site must use `formatRegNo()`, never read `.registrationNo` directly** — a raw read would silently regress to printing `"Reg. no. null"`.
- This closes Open question #1's "renaming the two `MOCK-`-prefixed doctor page filenames" concern from the S8 entry above — moot now, since those files no longer exist rather than needing a rename.

---

## Decision log

*Settled decisions. Check here before changing an earlier session's approach — don't re-litigate. If you do change one, add a row with the reason.*

| Date | Decision | Reason |
| :--- | :--- | :--- |
| 2026-08-16 | Direction is **"Quiet Light"** — laser as warm amber light, not cold clinical blue | The site's job is to lower an anxious visitor's heart rate. Cold precision aesthetics make a scary word scarier. |
| 2026-08-16 | Anchor dark is deep surgical green `#12302A`, not navy or black | From the operatory itself; warmer than black; near-complement of amber |
| 2026-08-16 | No red in the palette | Blood association undercuts a clinic selling bloodless procedures |
| 2026-08-16 | Signature element is the **Procedure Comparator** in the hero, not a photo or headline | Answers "will this hurt?" in five seconds, built from the real differentiator, needs zero patient imagery (sidesteps DCI) |
| 2026-08-16 | ⚠️ **Rebalanced to two pillars — Dental Implants + Gum/Periodontal Treatment — with laser as the *method*, not the identity** | Online verification: Dr. Sampath is MDS Oral Implantologist + Periodontist + LASER Specialist. Implants is first in the clinic's name and his qualifications; laser gingivoplasty is itself a periodontal procedure. The earlier laser-as-identity framing weighted a technique over two clinical specialisms. Supersedes any earlier laser-first guidance. |
| 2026-08-16 | Added `gum-treatment` as a treatment page; removed laser-first ordering | Periodontics is the principal's actual MDS specialism and had no page at all. Grid now orders by pillar, then search demand. |
| 2026-08-16 | Homepage gains a **two-pillar section** above the treatments grid | Positioning needs to happen before the full 11-card scope, or the two things that matter get lost among nine that don't |
| 2026-08-16 | Keep 3–4 doctors despite evidence of one | Human decision. Component handles any N; degrading a grid is cheaper than growing a single profile into one. |
| 2026-08-16 | Never publish a years-of-experience figure | Sources conflict (23+ vs 15; MDS 2010). Unverifiable experience claims are prohibited under the DCI code. Use "in practice since &lt;year&gt;" once confirmed. |
| 2026-08-16 | Type is Fraunces + Karla + IBM Plex Mono | Deliberately avoids the Playfair/Inter default pairing; Karla holds up at 15px on mid-range Android |
| 2026-08-16 | Static HTML/CSS/JS, no framework, no build step | Matches the repo's existing per-niche pattern; fast, cheap to host, nothing to patch |
| 2026-08-16 | Build output at `websites/akshaya/`, separate from `public/demo/dental/` | The demo is the sales asset for all dental leads; the client site is its own thing |
| 2026-08-16 | Build proceeds on **mock data** before the client call | Unblocks the build; content swaps are cheap, architecture changes are not. All mock values `MOCK:`-flagged in `content.js`. |
| 2026-08-16 | `showPricing` ships **false** | Client hasn't confirmed publishing cost ranges; block is built, just hidden |
| 2026-08-16 | Assumed 5 extra treatments beyond the client's stated 5 | Implants is in the clinic's own name; the rest are standard multispecialty scope. Flagged unconfirmed; cutting one costs nothing. |
| 2026-08-16 | No before/after gallery component exists in the system at all | DCI prohibits before/after of identifiable patients — this is a component-inventory constraint, not a copy constraint |
| 2026-08-16 | S1: mock doctor count fixed at 3, not 3–4 | CONTENT-DATA.md left it a range; a concrete number was needed to build `content.js`. Trivial to add a fourth object later — no template impact. |
| 2026-08-16 | S1: Fraunces variable font instanced to only `SOFT` + `wght` (400–600) axes; `opsz` pinned 48, `WONK` pinned 0. Karla shipped as one variable file (wght 400–700) instead of 3 statics | Matches the fixed SOFT/WONK values + wght range actually used in DESIGN-SYSTEM §3; smallest file for the axes CSS actually varies. Total font payload 98.6KB, under the 120KB budget. |
| 2026-08-16 | S1: reduced-motion handled by zeroing `--duration-*` tokens in `base.css`'s media query, not blanket `!important` overrides | CONVENTIONS.md §4 bans `!important` outright; token-driven durations let later sections' CSS honour reduced-motion automatically as long as they read `var(--duration-*)` instead of hardcoding timings. |
| 2026-08-16 | `.gitignore`'s blanket `websites/*` ignore now has an exception for `websites/akshaya/` | That rule was written for dynamically-generated demo output; this build is hand-authored client work and should be version-controlled. Committed in `a0d0cf8`. |
| 2026-08-16 | S2: added `--drape-deep` and `--whatsapp` tokens to `tokens.css` (footer band, WhatsApp brand green) | Both needed a raw hex not already covered by the S1 palette; CONVENTIONS.md §4 requires all raw hex to live in `tokens.css`, and DESIGN-SYSTEM §6 explicitly calls WhatsApp green "the one palette exception." |
| 2026-08-16 | S2: added `--header-height` and `--action-bar-height` tokens | Needed by multiple components (rail sticky offset, mobile menu close-button centering, section scroll-padding) — better as a shared token than repeated magic numbers. |
| 2026-08-16 | S2: header transparency is opt-in via `body[data-header-mode="transparent"]`, not the default | Only the homepage hero needs a transparent-over-image header; every inner page wants it solid from load. Keeps the transparent CSS scoped instead of fighting it back to solid everywhere else. |
| 2026-08-16 | S2: `data-bind`/`data-cta` elements ship with empty text/`href="#"` in markup, not a real-value fallback | CONVENTIONS.md §3's hardcoding ban applies even to fallback text — a duplicated real value in 13 pages' markup breaks the single-file content swap the whole system is built around. |
| 2026-08-16 | S3: `index.html`'s `<body>` does **not** carry `data-header-mode="transparent"`, despite S2's note that the homepage is the intended user of it | S3's hero ground is `porcelain` (light) per DESIGN-SYSTEM §5 row 1 — the transparent-header CSS swaps header text to `--porcelain` (white) for a dark/image hero to sit under, which on a light hero would render near-invisible white-on-near-white nav text. No dark or image passage exists at the top of this hero, so solid-from-load is the only contrast-safe option here. The transparent mode itself is untouched in `components.css` and stays available for a future page with an actual dark/image hero. |
| 2026-08-16 | S3: comparator hero split is 45/55 only at ≥1024px; 768–1023px stays stacked (single column) rather than also splitting | DESIGN-SYSTEM §9's 768–1023 row explicitly allows "stays stacked" as a fallback "if the comparator has room, else—"; the comparator card plus its illustration needs more than half of a 768px viewport to read cleanly, so stacking was chosen over a cramped split. |
| 2026-08-16 | S3: comparator toggle state, illustration, and row copy are driven by the component's own markup/JS, not `content.js` | Per CONTENT-DATA.md §4, `content.js`'s shape has no comparator model — the rows are generic laser-vs-conventional procedure facts (see Carried forward), not client-specific data, so CONVENTIONS §3's hardcoding ban doesn't apply to them the way it does to name/phone/address/doctor data. |
| 2026-08-16 | S3: page-load motion sequence hides its initial (pre-animation) state only inside `@media (prefers-reduced-motion: no-preference)` in `sections.css`, rather than hiding unconditionally and un-hiding via a JS-added class for reduced-motion users | Guarantees reduced-motion users never see a hidden/opacity-0 state at all — no dependency on JS timing or a fallback class to avoid a flash of invisible content, satisfying CONVENTIONS §6's "the page must lose nothing but movement" more robustly than a JS-toggled override. |
| 2026-08-16 | S4: treatment card has **no** numeric index, despite DESIGN-SYSTEM §6 describing one | CONVENTIONS.md §5 explicitly restricts 01/02/03-style numbering to the treatment step process alone. Treated CONVENTIONS as authoritative over the component-inventory description per its own framing ("the rules you must not break"). |
| 2026-08-16 | S4: "Why laser" section's four items are written as short attribute headline + one factual sentence each ("Light, not a blade," "Seals as it works," "Anaesthesia, often none," "Single-sitting disinfection") rather than outcome-flavoured headlines like "Reduced bleeding" | First draft used outcome-adjacent headline language; rewritten to name what the laser *does*, per CONVENTIONS §1's does-vs-gets test, before shipping. |
| 2026-08-16 | S4: rating monument arc fill is a static computed value (`stroke-dashoffset: 0.02`), not derived from `clinic.rating` at runtime | Simplest correct implementation for a single fixed rating; see Carried forward for the manual-update caveat if the real rating changes. |
| 2026-08-16 | S4b: two-pillar panels are generated in `main.js` from `treatments.find()` on the `dental-implants`/`gum-treatment` slugs, not a separate `content.js` data shape | CONTENT-DATA.md §4 has no pillars model; reusing the treatments entries as the single source keeps the pillar panel copy and the treatments-grid card copy for the same two treatments from drifting apart. |
| 2026-08-16 | S4b: pillar panels carry a plain-text category label ("Implants" / "Gum & periodontal"), not a 01/02 index | CONVENTIONS.md §5 restricts 01/02/03 numbering to the treatment step process alone; a numbered pillar panel would violate that. |
| 2026-08-16 | S4b: `dental-implants.isLaser` flipped `false` → `true` | DESIGN-SYSTEM.md §1 states both pillars are laser-delivered. Flagged in Carried forward as unconfirmed protocol detail — revert if the client says otherwise. |
| 2026-08-16 | S4b: kept the `.why-laser` CSS class name in `sections.css`/`index.html` despite the section's visible heading/label changing to "How we work — laser" | Renaming the CSS class would touch every rule in that block for a purely internal identifier with no user-facing effect; the visible copy (eyebrow, h2, rail label) is what BUILD-STATE's rename instruction was actually about. Flag if a future session wants the class renamed for consistency. |
| 2026-08-16 | S5: `doctors[]` rewritten to lead with the real, verified Dr. Sampath Kumar Rao K (`isPrincipal: true`) + 2 MOCK associates (3 total), dropping the old all-mock "Dr. Vikram Shetty" entry | S1 never actually added the verified principal from `CONTENT-DATA.md` §1/§4 — a gap, not a prior decision. Vikram's implants+gum-treatment+laser coverage is now redundant with Sampath's real specialisation. |
| 2026-08-16 | S5: `doctors[].experienceYears` (numeric) replaced with `practisingSince: null` across all doctors, left unrendered | Matches `CONTENT-DATA.md` §4's canonical shape; CONVENTIONS §1 bans rendering any years-of-experience figure, and the two public sources for Dr. Sampath conflict (23+ vs 15 years). |
| 2026-08-16 | S5: doctor card name renders as Karla `h3`, not Fraunces | `DESIGN-SYSTEM.md` §6's component inventory says Fraunces, but §3's Restraint rule explicitly scopes Fraunces to `h1`/`h2`/pull-quotes/rating monument and calls out `h3` as Karla by name. Treated the Restraint rule as authoritative, same resolution pattern as S4's CONVENTIONS-vs-component-inventory call on step numbering. |
| 2026-08-16 | S5: added `equipment[]` and `sterilisation[]` as new top-level `content.js` exports (both `MOCK:`-flagged) | Needed for the homepage Technology & sterilisation section; no such shape existed in `CONTENT-DATA.md` §4's sketch. Modeled identically to `treatments[]`/`doctors[]` — array of objects, single source. S8's full Technology page should reuse these, not add a second shape. |
| 2026-08-16 | S5: "Patient stories" renders static review cards from `content.js`, with no live Google embed wired in | This *is* the "static fallback" S5's scope calls for — a real live embed needs an API key or third-party script the build doesn't have yet, and would be the build's first external dependency. Left for a future session to decide deliberately, not defaulted into. |
| 2026-08-16 | S5: "Visiting the clinic" map is a `placeholder-block`, not an embedded iframe | `clinic.address.geo`/`mapsUrl` are still MOCK/null (Open question #9) — nothing real to embed yet. Follows the same labelled-placeholder convention as doctor/equipment photos. |
| 2026-08-16 | S6: treatment pages generated by a one-off local Python script, then deleted — not a shipped build step | Static HTML/no-build-step (CONVENTIONS §2) means the *site* can't require a build step to run; authoring the 6 near-identical files with a script instead of by hand is a one-time authoring convenience, not a runtime dependency. All client-specific content still comes from `content.js` at runtime, same as every other generated section on the site. |
| 2026-08-16 | S6: treatment pages use root-relative paths (`/assets/...`, `/index.html`) instead of `index.html`'s page-relative style | Matches `main.js`'s existing root-relative footer treatment links (since S2); avoids `../` counting as page depth grows. `index.html` itself left unchanged — inconsistency flagged in Carried Forward, not fixed here (out of scope). |
| 2026-08-16 | S6: comparator rows kept identical across all 5 embedding pages rather than writing procedure-specific attribute values | Inventing new specific claims (e.g. a made-up suture rate for implants specifically) without a verified source risks an unverifiable-claim compliance problem; the existing rows are the pre-vetted generic reference copy. Revisit if real procedure-specific data arrives from the client call. |
| 2026-08-16 | S6: `<title>`/`<meta description>` on treatment pages are hand-written per file, not data-bound | Static HTML needs a real `<title>` for non-JS crawlers/social previews; JS can't populate it in time. Explicitly S11's job to systematize (SESSION-PLAN.md). Manual edit required if a treatment's `content.js` name changes before then. |
| 2026-08-16 | S6: aftercare lists reuse the S5 `.sterilisation-list` checkmark-list classes rather than new CSS | It's a generic checkmark list with no protocol-specific styling; avoided a near-duplicate block. Class names are now a little misleading outside their original context — cosmetic, not urgent. |
| 2026-08-16 | S7: treatment-card markup factored into a shared `treatmentCardHtml()` helper in `main.js`, used by both the homepage grid and the new hub grid | Two independent call sites generating the same card markup as separate literal strings was the exact "content.js-only edit" risk CONVENTIONS §3 warns about — a copy/paste drift waiting to happen. |
| 2026-08-16 | S7: treatments-hub pillar grouping is hand-written in `main.js`, not a `content.js` field | No pillar model exists in CONTENT-DATA.md §4; same resolution S4b used for the homepage's two-pillar section. Revisit if a future page (e.g. About) also needs pillar grouping — worth promoting to a real `content.js` field at that point. |
| 2026-08-16 | S7: added a "Related treatments" section to the shared treatment template, touching all 11 pages including the 6 already shipped by S6 | Explicitly named in SESSION-PLAN.md's S7 scope ("related-treatment links between pages"); `related[]` has existed unused in `content.js` since S1. Not scope creep — a named deliverable that happens to touch prior sessions' files. |
| 2026-08-16 | S7: added `alsoOffered` as a new top-level `content.js` export, rendered via the existing `.chip` component | Follows the S5 precedent (`equipment[]`/`sterilisation[]`) of adding a new export rather than a second data shape; reusing `.chip` avoided new near-duplicate list-item CSS. |
| 2026-08-16 | S8: individual doctor pages generated as static `team/<slug>.html` files, one per `doctors[]` entry, same one-off-script pattern as S6/S7's treatment pages | `flags.showDoctorPages` calls for "individual pages when a doctor has enough content"; a data-bound static file per doctor matches the site's existing static-HTML-plus-`content.js` architecture, no new pattern introduced. |
| 2026-08-16 | S8: sterilisation workflow rendered twice — as `.step-process` on `technology.html`, as `.sterilisation-list` (checklist) on `about.html` — from the same `sterilisation[]` array, no new content.js shape | CONTENT-DATA.md has no separate "workflow" vs "checklist" model; reusing the one array for two presentations avoided inventing parallel data just to satisfy two different visual specs (SESSION-PLAN's "step process" for Technology vs "full protocol" for About). |
| 2026-08-16 | S8: added a "Technology" link to the header/mobile-nav/footer of all 13 pre-existing pages, not just the 3 new ones | `technology.html` needed at least one inbound link to not be an orphan page; nav/footer are site-wide chrome (S2's scope), so a new top-level page necessarily touches every page that carries that chrome. Judged in-scope for S8 rather than deferred to Carried Forward, since an unreachable page defeats the point of building it. |
| 2026-08-16 | S9: booking form's valid-submit path shows a static client-side confirmation panel instead of actually sending the submission anywhere | No form handler exists yet — SESSION-PLAN.md explicitly assigns "Form handler + WhatsApp notification path" to S11. Building full validation now, with the send-mechanism as the one deliberately-deferred piece, matches the "static fallback" pattern S5 used for the reviews embed. |
| 2026-08-16 | S9: "preferred day" field is a native `<input type="date">`, not a day-of-week select | CONVENTIONS §1 names the field "preferred day" without specifying its input type; a date picker is simpler than inventing a day-of-week+time model and gives the clinic an unambiguous date to work from when confirming by phone/WhatsApp. |
| 2026-08-16 | S9: treatment-interest options are generated from `treatments[]` (sorted by `order`) plus one hand-written "Not sure yet" option, not a `content.js` field | Same reasoning as S7's treatments-hub pillar grouping — small structural additions that aren't client facts don't need to become data; the option list itself still can't drift from the treatments the clinic actually offers, since the treatment names come from `content.js`. |
| 2026-08-16 | S9: privacy policy body text is static prose in `privacy-policy.html`, not a `content.js` field | Generic DPDP-notice legal language, not Akshaya-specific client data — same resolution S3 used for the comparator's row copy under CONVENTIONS §3. |
| 2026-08-16 | S10: blog post pages replace the standard rail (`.rail__label` + scroll-% track) with a table-of-contents mode (`.rail__toc`, a list of anchor links tracked by its own `IntersectionObserver`) | DESIGN-SYSTEM §7 explicitly specifies a sticky TOC in the rail for the post template, not the section-label rail every other page uses. Scoped to post pages only — other pages' rail behaviour is untouched. |
| 2026-08-16 | S10: `blogPosts[].title`/`excerpt`/`coverLabel`/every `body[].paragraphs[]` string all carry a literal `MOCK:` prefix rendered as-is | CONTENT-DATA §3 lists blog posts as fully invented. Followed the `doctors[]` MOCK-associate precedent (prefix renders in the live UI) rather than the treatment-name precedent (verified names, no prefix), since no part of any post is a verified client fact. |
| 2026-08-16 | S10: each post's `body` is `[{id, heading, paragraphs[]}]`, a single array driving the rendered post content, the rail TOC, and the TOC's anchor targets | One source for three views avoids the TOC drifting out of sync with the post's actual sections if a future edit adds/removes/renames one. |
| 2026-08-16 | S11: JSON-LD schema is injected at runtime by a new `schema.js`, not written as static `<script type="application/ld+json">` per page | Keeps every schema field data-bound to `content.js` (CONVENTIONS §3's single-file-edit rule extended to structured data) instead of a second hardcoded copy of the same facts in 29 files. Google's crawler executes JS before evaluating structured data, so this is supported for Rich Results. |
| 2026-08-16 | S11: canonical/OG/Twitter `<meta>` tags are static per-file (via a one-off script, then deleted), not JS-injected like the schema | These need to be present for crawlers/social scrapers that don't execute JS — same reasoning S6 used for `<title>`/meta description staying hand-written rather than data-bound. |
| 2026-08-16 | S11: `clinic.siteUrl` is a plausible placeholder domain (`akshayadentalclinic.in`), not left null | Canonical URLs, OG tags, JSON-LD `@id`/`url`, `sitemap.xml`, and `robots.txt` all need *some* absolute URL to be structurally valid and testable; a null/empty value would leave 29 pages with broken canonical tags. Flagged as needing a real-domain swap once Open question #12 resolves, tracked in Carried forward — not `MOCK:`-prefixed since it's consumed as a URL, not rendered as text. |
| 2026-08-16 | S11: booking form's real handler is Netlify Forms (fetch POST to `/` with `data-netlify` static markup), not Formspree | SESSION-PLAN.md named either as acceptable; Netlify Forms needs zero third-party account/API key to build against (Formspree requires a form ID issued after signup, which doesn't exist yet), and PROJECT-BRIEF §6 already names Netlify as the preferred host. Revisit if the client's eventual hosting choice is Cloudflare Pages instead (Decision log has no hosting choice yet — still Open question #12). |
| 2026-08-16 | S11: WhatsApp "notification path" is a client-side pre-filled `wa.me` link on the confirmation panel, not a server-side webhook | A real webhook needs Netlify Forms' outgoing-notification settings plus a third-party integration (Zapier/Make/Twilio), which is dashboard configuration done after deployment, not something a static codebase can wire in advance. The client-side fallback works immediately and needs no post-deploy configuration to have some effect. |
| 2026-08-16 | S11: favicon is a generated mark reusing the existing beam-mark motif (diagonal amber line + circle, drape ground), not a text/monogram or a generic default icon | No real logo exists yet (Open question #10). Reusing an existing component's visual language (the laser treatment-card mark) keeps the placeholder consistent with the rest of the system instead of introducing an arbitrary new shape that would need to be designed twice. |
| 2026-08-16 | S11: no `og:image`/`twitter:image`/schema `image` field anywhere | No real photography exists yet — every image on the site is an unrendered `.placeholder-block` div, not a file. Referencing a nonexistent image path is worse than omitting the field; add once real photography lands (Open question #11). |
| 2026-08-17 | S12: image conversion/lazy-loading/explicit-dimensions scope item was a no-op — verified, not skipped | Full codebase grep found zero `<img>` tags anywhere; every image is still a `.placeholder-block`. Nothing to convert or add attributes to until real photography lands (Open question #11). |
| 2026-08-17 | S12: `.mobile-menu[hidden] { display: none; }` added, no JS change | Same bug class as S9's `.booking-form[hidden]` fix — a component with its own `display` declaration defeats the native `hidden` attribute regardless of specificity, since author CSS beats the UA stylesheet outright. `main.js` already toggled the `hidden` attribute correctly; only the missing CSS rule was the bug. |
| 2026-08-17 | S12: rating monument arc gets its own `prefers-reduced-motion: reduce` block setting the final `stroke-dasharray`/`stroke-dashoffset` directly, instead of leaving those properties unset outside `no-preference` | Leaving them unset rendered a full undashed circle (= a false 5.0-of-5 visual) for reduced-motion users instead of the correct ~98% (4.9-of-5) arc. CONVENTIONS §6's "SVG arcs render complete" means "in their correct final state," not "as an unclipped full circle." |
| 2026-08-17 | S12: comparator toggle buttons and footer social icons resized from raw `40px`/`36px` literals to `var(--space-12)` (48px) | Both were below the CONVENTIONS §7 48×48px tap-target floor and were themselves raw-px-literal violations of the tokens.css-only rule; fixing the size with an existing token fixed both problems in one edit. |
| 2026-08-17 | S12: footer treatment-links list and inline contact text-links left below 48×48px, not resized | Site-wide pattern since S2 across all 29 pages, not a single-page bug — a proper fix is a deliberate footer-density redesign decision, out of a QA session's scope. Flagged in Carried forward instead of touched. |
| 2026-08-17 | S12: `.booking-form__submit` wrapper added around `#bookingSubmitError` + the submit button, with its own tighter `var(--space-2)` gap | The error paragraph was a direct child of `.booking-form`'s 24px flex gap on both sides, reserving a dead ~64px gap before the submit button even when empty. Grouping it with the button at an 8px inner gap matches the tighter per-field error spacing pattern already used elsewhere and removes the uneven rhythm — this was S11's flagged, not-yet-eyeballed item. |

---

## Open questions for client

*Raised during the build, to be resolved on the discovery call. Full questionnaire in `PROJECT-BRIEF.md` §7.*

| # | Question | Blocks |
| :-- | :--- | :--- |
| 1 | Exact number of doctors, with names, degrees, specialisations, and **real DCI registration numbers** | launch (built for S8's `team/` pages; real associate count/names would also mean renaming the two `MOCK-`-prefixed doctor page filenames, see Carried forward) |
| 2 | Which of the 5 assumed treatments do they actually offer? Any we've missed? | S7 |
| 3 | Publish indicative cost ranges? EMI partners? | `showPricing` flag |
| 4 | Exact clinic timings per day + emergency/after-hours policy | S5 |
| 5 | Founding year, clinic story, what they want to be known for | launch (`about.html` built on MOCK `about.story`/`about.philosophy`, S8) |
| 6 | Equipment list with model names | launch (`technology.html` built on MOCK `equipment[]`, S8) |
| 7 | Sterilisation protocol specifics (autoclave class, single-use policy) | launch (rendered on both `about.html` and `technology.html`, S8, from MOCK `sterilisation[]`) |
| 8 | Official email for form submissions | launch (Netlify Forms notification routing needs a real inbox once deployed, S11) |
| 9 | Google Business Profile access + exact Maps listing URL and geo coordinates | S5, launch (schema.js's `geo`/`sameAs` fields stay empty without it, S11) |
| 10 | Logo source file (SVG/AI preferred) | S1, launch (`favicon.svg` is a generated placeholder mark, S11 — see Carried forward) |
| 11 | Photography — who shoots it, when | Launch (also blocks `og:image`/`twitter:image`/schema `image`, S11) |
| 12 | Domain choice | Launch (blocks `clinic.siteUrl`, every canonical/OG/JSON-LD URL, `sitemap.xml`, `robots.txt`, and whether Netlify Forms' fetch endpoint is even reachable, S11) |
| 13 | Real cost ranges for all 11 treatments (S6: dental implants, gum treatment, laser gingivoplasty, root canal, laser root canal; S7 added crowns & bridges, dental restoration, braces & aligners, teeth whitening, kids dentistry — all currently MOCK figures behind the hidden `showPricing` flag) | `showPricing` flag, S6/S7 cost blocks |
| 14 | First-visit walkthrough, what-to-bring list, accepted payment methods, EMI partner terms, insurance handling, and the actual emergency/after-hours policy (`patient-information.html` built on MOCK `patientInfo`, S9) | launch |
| 15 | GA4 property ID (or should we create one under the client's own Google account?) | launch (`analytics.js` built and wired to all 5 conversion events, S11, but inert on the `MOCK: G-XXXXXXXXXX` placeholder) |
| 16 | Confirm hosting: Netlify vs. Cloudflare Pages | launch (booking form's real handler is Netlify-Forms-specific, S11 — a Cloudflare Pages choice would need `form.js`'s submit handler rebuilt against a different backend) |

---

## Session log

*One entry per session. Append, never overwrite.*

### 2026-08-16 — Planning
Research, commercial brief, and design system completed. Repo restructured: client docs moved into `clients/akshaya-dental/`, output folder `websites/akshaya/` created, root `CLAUDE.md` added to auto-route future sessions here. No code written.

### 2026-08-16 — S1 Foundation
Built the system all later sessions render on top of. Folder structure per DESIGN-SYSTEM §11; `tokens.css` with every color/type-step/space/radius/duration as a custom property (no raw literals elsewhere); `base.css` with reset, self-hosted `@font-face` declarations, heading defaults, focus rings, and a token-driven `prefers-reduced-motion` block. Downloaded, instanced, and glyph-subset the three fonts to Latin with `fontTools` (98.6KB total, under the 120KB budget) — see Decision log for the axis choices. Wrote the full `content.js` data object per CONTENT-DATA.md §4 with all mock values `MOCK:`-flagged, including all 10 treatments, 3 mock doctors, 8 FAQs, 3 mock reviews. Built `_kitchen-sink.html` and verified it in a real headless-browser render at 1440 and 375 widths — all three fonts report `loaded` via the Font Loading API, zero console errors, zero failed network requests, both grounds and full type scale render correctly.

**Files created:** `assets/css/tokens.css`, `assets/css/base.css`, `assets/js/content.js`, `_kitchen-sink.html`, `assets/fonts/{fraunces-variable,karla-variable,ibm-plex-mono-400,ibm-plex-mono-500}.woff2`, empty `treatments/` and `blog/` dirs, `assets/img/{doctors,facility,equipment,blog}/` dirs.

**Done-when check:** kitchen sink renders all tokens correctly ✅ (screenshotted at desktop + mobile) · no literal hex outside `tokens.css` ✅ (grepped) · fonts load locally with no network calls ✅ (requestfailed listener empty, all font URLs relative) · total font payload under 120KB ✅ (98.6KB).

**Files created/changed:** `CLAUDE.md`, `clients/akshaya-dental/{00-START-HERE,SESSION-PLAN,CONVENTIONS,CONTENT-DATA,BUILD-STATE}.md`, moved `PROJECT-BRIEF.md` + `DESIGN-SYSTEM.md` into `clients/akshaya-dental/`.

### 2026-08-16 — S2 The frame
Built the site-wide chrome every later page assembles into: `components.css` (buttons incl. the WhatsApp brand-green exception, pills, chips, hairline, header, mobile overlay menu, sticky left rail + scroll fill, mobile progress line, sticky action bar, WhatsApp float, footer) and `main.js` — the first real `content.js` consumer, binding via `data-bind`/`data-cta`, driving header solid-on-scroll, the hamburger/overlay menu (focus management, Escape-to-close, keyboard operable), rail label tracking via `IntersectionObserver`, scroll-progress fill, and generating the footer's treatment links and hours list straight from `content.js`. Added the header/rail/menu/action-bar/WhatsApp-float/footer markup to `_kitchen-sink.html` so the done-criteria could be verified against a real page. Added two tokens to `tokens.css` (`--drape-deep`, `--whatsapp`) and two layout tokens (`--header-height`, `--action-bar-height`).

Verified with a real headless-browser render (Puppeteer) at 360 / 768 / 1440px: nav/rail/WhatsApp-float appear only ≥1024px and ≥768px respectively per spec, action bar only <768px, footer treatment list (10) and hours (7, today highlighted) generate correctly from `content.js`, all `tel:`/`wa.me` links resolve with the right numbers, hamburger menu opens/closes with correct `aria-expanded`/focus movement and Escape support, desktop tab order reaches every nav link, and `prefers-reduced-motion` correctly zeroes all duration tokens. Caught and fixed a real bug in the process: `.action-bar` and `.site-footer` set dark backgrounds directly but were missing `data-ground="drape"`, so `--fg`/`--fg-muted` stayed at light-ground values and text rendered dark-on-dark (invisible except for the one hardcoded-color WhatsApp item). Also caught that my own markup was hardcoding real confirmed client values (phone, name, address) as `data-bind` fallback text — removed per CONVENTIONS.md §3, elements now ship empty and are populated by `main.js`.

**Done-when check:** header, footer, rail, action bar render on kitchen sink at 360/768/1440 ✅ · nav keyboard operable ✅ (tab order + hamburger focus verified) · no client data hardcoded ✅ (grepped, and the fallback-text bug above was caught and fixed).

**Files created/changed:** `assets/css/components.css`, `assets/js/main.js`, `assets/css/tokens.css` (added `--drape-deep`, `--whatsapp`, `--header-height`, `--action-bar-height`), `_kitchen-sink.html` (header/rail/menu/action-bar/WhatsApp-float/footer markup added).

### 2026-08-16 — 🚩 S3 Hero + Procedure Comparator

Built `index.html` — the first real page, assembling the S2 frame around the hero and the signature Procedure Comparator. Hero: eyebrow (locality), `h1` (clinic name), lead paragraph (tagline), rating pill, and a CTA pair (Book / WhatsApp), all bound from `content.js` via the existing `main.js` pattern. Desktop splits 45/55 at ≥1024px; stays stacked at 768–1023px (see Decision log); mobile stacks with the primary CTA visible above the fold at 360×640.

Comparator (`comparator.js` + markup in `index.html`): a real `role="radiogroup"` segmented Conventional/Laser toggle, keyboard-operable (arrow keys move focus and selection), `aria-live="polite"` region announcing state changes, four rows (Drill used, Sutures, Typical sittings, Anaesthesia) crossfading their values, an inline SVG tooth illustration with two crossfaded overlay layers (drill+sutures vs. amber beam+glow), and the required suitability disclaimer. Row copy is DESIGN-SYSTEM §6's own vetted example, not `content.js`-modeled (see Carried forward). Instant swap under `prefers-reduced-motion`.

`motion.js`: the 5-beat page-load sequence (ground fade → heading word-stagger → meta/CTA fade-up → comparator fade-up + SVG trace via `pathLength="1"` + `stroke-dashoffset` → rail hairline draw), gated entirely inside `@media (prefers-reduced-motion: no-preference)` in `sections.css` so reduced-motion users never see a hidden initial state. Also exports and calls the reusable `initScrollReveal()` IntersectionObserver utility (`[data-reveal]`/`.is-revealed`, once-only) that S4+ will use for section reveals — unused this session since only one section exists.

New `sections.css` (hero, comparator, load-sequence, scroll-reveal) and a `.visually-hidden` utility added to `base.css` for the comparator's live region. `index.html`'s `<body>` omits `data-header-mode="transparent"` (see Decision log — this hero's ground is light, transparent mode assumes a dark/image hero).

Verified with Playwright at 360×640 / 768×1024 / 1440×900: zero console errors, zero failed requests, no horizontal overflow, primary CTA visible without scrolling at 360×640, comparator toggles correctly by click and by arrow-key with correct `aria-checked`/`aria-live` updates, and reduced-motion context shows full opacity with no hidden state. Screenshotted both comparator states at all three widths.

**Done-when check:** hero + comparator work at 360/768/1440 ✅ · toggle keyboard operable and screen-reader sane ✅ (`role="radiogroup"`, arrow-key navigation, `aria-live`) · reduced-motion path verified ✅ · no outcome claims anywhere ✅ (rows are procedure attributes only, disclaimer present).

**Files created/changed:** `index.html`, `assets/css/sections.css`, `assets/js/comparator.js`, `assets/js/motion.js`, `assets/css/base.css` (added `.visually-hidden`).

**🚩 Stop here for human review before S4.**

### 2026-08-16 — S4 Homepage part A

Added the next three homepage sections to `index.html`, each with its own `data-rail-label`: the rating monument, the treatments grid, and "Why laser" (the first `drape` section on the page).

**Rating monument** (bisque ground): `4.9` in Fraunces at `--step-6`, centered over an SVG ring — a static track circle plus an amber `stroke-dashoffset`-animated fill circle that draws to ~98% (4.9-of-5) on scroll-in, gated by the existing `[data-reveal]`/`initScrollReveal()` system from S3. `1,465 verified Google reviews` beneath in Plex Mono, both values bound from `content.js` via the existing `data-bind` pattern.

**Treatments grid** (porcelain ground): all 10 treatments generated in `main.js` from `content.js`, sorted by the `order` field (already laser-first per CONTENT-DATA.md). Each card: amber beam-mark SVG (laser treatments only), title, one-line (2-line-clamp) summary, sittings chip, arrow. 1/2/3-up responsive at 360/480/1024. No numeric index — see Decision log, this conflicts with DESIGN-SYSTEM §6's stated component but CONVENTIONS §5 is authoritative.

**"Why laser"** (drape ground, first of the page's max-two dark sections): eyebrow + heading + a 4-item grid of procedure-attribute cards (rewritten once during the session to strip outcome-flavoured phrasing — see Decision log) + the suitability disclaimer, all revealing via `[data-reveal]`. A radial-gradient glow div drifts slowly (`--duration-ambient: 20s`, new token) behind the content, entirely inside `@media (prefers-reduced-motion: no-preference)` per the established motion-gating pattern.

Verified with Playwright at 360×640 / 768×1024 / 1440×900: zero console errors, zero failed requests, no horizontal overflow, all 10 cards render from data, rating figure shows "4.9" correctly, reveal system fires correctly on scroll (confirmed directly with `scrollIntoView` after a `fullPage` screenshot showed a scroll-simulation-timing artifact — see Carried forward on `fullPage` + fixed-position elements). Screenshotted all three widths.

**Done-when check:** three sections render and reveal correctly at all breakpoints ✅ · treatment cards generated from data, not hand-written ✅ (grepped `index.html`, confirmed no hardcoded treatment markup) · exactly one dark section used so far ✅ ("Why laser" is the first; hero and the other two S4 sections are light).

**Files created/changed:** `index.html` (3 sections added), `assets/css/sections.css` (rating monument, treatments grid/card, why-laser section, reveal-stagger rules), `assets/js/main.js` (treatments-grid generator), `assets/css/tokens.css` (added `--duration-ambient`).

### 2026-08-16 — S4b Rebalance rework

Corrected S3/S4's laser-as-identity framing to the two-pillar positioning established in the docs after online verification (`CONTENT-DATA.md` §1). No teardown — palette, type, layout, motion, and the comparator itself are untouched.

**Hero:** `clinic.tagline` in `content.js` rewritten to name both pillars ("Dental implants and gum treatment, both delivered with laser") — the hero markup itself didn't change, since the lead paragraph under the h1 was always the right slot for this copy, only its content was laser-only before.

**New "Two pillars" section** (`index.html`, between rating monument and treatments grid, porcelain ground, `data-rail-label="The two pillars"`): two panels generated in `main.js` from the `treatments[]` entries for `dental-implants`/`gum-treatment` (name + summary reused directly, so the panel and the treatments-grid card for the same treatment can't drift apart). New `.pillar-panel` styles in `sections.css` — bisque card, amber left-edge accent revealed on hover, category label instead of numeric index (CONVENTIONS §5 restricts 01/02 numbering to the step process only).

**`content.js`:** added `gum-treatment` (order 2, isLaser true, confirmed true); reordered all 11 treatments pillar-then-demand per CONTENT-DATA.md §5's table (was laser-first); flipped `dental-implants.isLaser` to `true` and `confirmed` to `true`; flipped `confirmed` to `true` on the 4 other treatments CONTENT-DATA now lists as verified (crowns-and-bridges, braces-and-aligners, teeth-whitening, kids-dentistry). Grid went 10 → 11 cards with no markup change — `main.js` regenerates it from `content.js.order`, confirming the "content.js-only" claim in the handoff.

**"Why laser" → "How we work — laser":** visible copy changed (eyebrow "How we work", h2 "How laser fits into your implant and gum treatment", `data-rail-label`) to frame laser as method, not identity. The four attribute items and disclaimer were already factual/procedure-only and needed no rewrite. CSS class name `.why-laser` kept as-is — internal identifier, not visible; see Decision log.

Verified with Playwright at 360×640 / 768×1024 / 1440×900: zero console errors, zero failed requests, no horizontal overflow, 11 treatment cards and 2 pillar panels render from data, both pillar panels reach `.is-revealed` on scroll, rail label correctly reads "How we work — laser" over that section, hero h1/lead text confirmed correct. Screenshotted hero, pillars section, and "how we work" section at 1440 and 360.

**Done-when check (from the handoff, not a formal session-plan entry):** hero names both pillars ✅ · two-pillar section present between rating monument and treatments grid ✅ · `gum-treatment` added and grid reorders pillar-then-demand, content.js-only change ✅ · "Why laser" reframed as method ✅ · re-screenshotted ✅.

**Files created/changed:** `assets/js/content.js` (tagline rewrite; `gum-treatment` added; treatments reordered; `isLaser`/`confirmed` flags updated), `index.html` (two-pillar section added; why-laser section copy + rail label changed; `<title>` updated), `assets/js/main.js` (pillars generator added), `assets/css/sections.css` (`.pillars`/`.pillar-panel` styles added).

**🚩 This was a correction session, not a review gate — flag to the human before S5 if they want to see it, but it doesn't block starting S5.**

### 2026-08-16 — S5 Homepage part B

Added the final 6 sections to `index.html`, completing the homepage: Meet the dentists, Technology & sterilisation, Patient stories, Visiting the clinic, Questions (FAQ), and the closing CTA band (drape ground, second and final dark section on the page).

**Meet the dentists:** fixed a real gap carried from S1 — `content.js`'s `doctors[]` had never actually included the verified real doctor. Rewrote it to lead with Dr. Sampath Kumar Rao K (`isPrincipal: true`, renders the wider `--principal` card with a bio) + 2 MOCK associates. Replaced the numeric `experienceYears` field with `practisingSince: null` per `CONTENT-DATA.md`'s canonical shape — never rendered, per CONVENTIONS §1. Doctor names render as Karla `h3`, resolving a Fraunces-vs-Restraint-rule conflict inside DESIGN-SYSTEM.md itself in favour of the Restraint rule (see Decision log).

**Technology & sterilisation** (bisque ground): added `equipment[]` (4 items) and `sterilisation[]` (4 protocol lines) as new `MOCK:`-flagged `content.js` exports, generated into equipment cards + a checkmark list.

**Patient stories** (porcelain): `reviews[]` rendered as static cards — this is the "static fallback" the session scope calls for; no live Google embed exists in this build yet (would need an API key/third-party script, the build's first external dependency).

**Visiting the clinic** (bisque): a `placeholder-block` map (no real geo/mapsUrl yet), address, `parking`/`languages` bindings (newly wired into `main.js`'s bindings map), directions/call CTAs, and a timings list with today highlighted (reusing the footer's day-highlight pattern).

**Questions** (porcelain): FAQ accordion from `content.js`'s existing `faqs[]`, using native `<details>`/`<summary>` with a `grid-template-rows` 0fr→1fr animation — author CSS overriding the UA's closed-`<details>` `display: none` needs no `!important` since author rules beat UA rules on specificity ties.

**Closing CTA band** (drape, second/final dark section): heading, three CTAs (Book/WhatsApp/Call), phone + locality line, reusing the "why-laser" section's ambient-glow keyframe/token.

Also added a shared `.placeholder-block` utility to `components.css` (flat bisque, dashed border, Plex Mono label) for the doctor/equipment/map placeholders, replacing what would otherwise have been three near-duplicate ad hoc styles.

Verified with Playwright at 360×640 / 768×1024 / 1440×900: zero console errors, zero failed requests, no horizontal overflow at any width, all six sections render their full data counts (3 doctors, 4 equipment, 4 sterilisation items, 3 reviews, 8 FAQs, 7 hours rows), scroll-reveal fires correctly per section, the FAQ accordion opens/closes on click, exactly two `drape` sections on the page (how-we-work-laser + cta-band). Screenshotted every new section at mobile and desktop widths.

**Done-when check:** homepage complete top to bottom ✅ · responsive at 360/480/768/1024/1440 ✅ (360/768/1440 directly verified; 480/1024 covered by the same fluid/grid rules already exercised in S4) · no horizontal overflow at any width ✅.

**Files created/changed:** `assets/js/content.js` (`doctors[]` rewritten with real principal + `practisingSince`; `equipment[]`/`sterilisation[]` added), `assets/js/main.js` (6 new section generators; `parking`/`languages` added to bindings), `assets/css/sections.css` (`.dentists`/`.doctor-card`, `.technology`/`.equipment-card`/`.sterilisation-list`, `.stories`/`.review-card`, `.visiting`, `.questions`/`.faq-item`, `.cta-band` styles added), `assets/css/components.css` (`.placeholder-block` added), `index.html` (6 sections added).

### 2026-08-16 — S6 Treatment template + confirmed treatments

Built the reusable treatment page template and shipped the 6 pillar + core treatment pages (CONTENT-DATA §5, rows 1–6): Dental Implants ⭐, Gum & Periodontal Treatment ⭐, Laser Gingivoplasty, Laser Dentistry (method hub), Root Canal, Laser Root Canal.

**`content.js`:** populated the previously-empty `signs[]`, `steps[]` (now `{title, desc}` objects), `aftercare[]`, and `faqs[]` (now `{q, a}` objects) arrays for all 6 treatments, plus a new `costRange: {min, max, unit, emi}` shape (MOCK, pending Open question #13) on 5 of them — `laser-dentistry` keeps `costRange: null` since it's a method hub, not a billable procedure. All new copy follows CONTENT-DATA §6 (write-it-as-if-it-ships) and CONVENTIONS §1 (procedure attributes only, no outcome claims).

**`assets/css/pages.css`** (new file, per CONVENTIONS §2's file list): breadcrumb, treatment hero, signs list, the **step process** (numbered 01→05 — the only component site-wide allowed that numbering, per CONVENTIONS §5 — vertical with a connecting line on mobile, horizontal on desktop), data block (sittings & duration as two Plex Mono stats), a comparator-section wrapper (reuses the existing generic `.comparator` component from `sections.css` unchanged), cost block (ships `hidden`, toggled by `flags.showPricing`), aftercare, treatment FAQs (reuses `.faq-list`/`.faq-item`), and the credential block (performing dentist).

**`assets/js/main.js`:** added a new "treatment page template" block (section 16) that populates every treatment-specific field from `content.js` via a `data-treatment-slug` attribute on `<body>` — name, H1, lead, sittings/duration chips, signs list, step process, data block, cost block (conditionally unhidden), aftercare, FAQs, and the performing-dentist credential block (first doctor whose `treatments[]` includes the page's slug, falling back to the principal). Also restructured `pageContext`/WhatsApp-message derivation to use the active treatment's name when `data-page-context` isn't set, so the WhatsApp pre-fill is correct on treatment pages without hardcoding it per file.

**6 HTML files** at `treatments/{dental-implants,gum-treatment,laser-gingivoplasty,laser-dentistry,root-canal,laser-rct}.html`, generated from a one-off local script (not shipped — see Decision log) sharing one template; only the `<title>`/`<meta description>` and the comparator block's presence differ per file. 5 of the 6 embed the S3 comparator (all except Root Canal, per CONTENT-DATA §5) with the same rows/values as the homepage's comparator (see Decision log on why they weren't written as new procedure-specific claims).

Verified with Playwright at 360×800 / 768×1024 / 1440×900 across all 6 pages: zero console errors, zero failed requests, no horizontal overflow at any width. Confirmed via direct DOM inspection that every generated block (signs, steps, aftercare, FAQs, credential block, comparator where present) populates with the correct counts, that Root Canal has zero comparator markup while the other 5 have exactly one, that the cost block ships `hidden` on all 6, and that the comparator toggle is keyboard-operable (arrow key moves focus, updates `aria-checked` and the `aria-live` region) on a treatment page, matching S3's behaviour. Diagnosed and resolved an apparent "blank page" false alarm from a `fullPage` screenshot — confirmed it was the known reveal/stitching artifact (see Carried Forward), not a real bug, via a reduced-motion capture and a real incremental-scroll capture, both showing full content.

**Done-when check:** 6 treatment pages live and consistent ✅ · template is genuinely reusable ✅ (one shared markup structure, all content-specific values from `content.js`) · every page carries the suitability disclaimer ✅ (in the hero) · breadcrumbs work ✅ (Home → Treatments → treatment name, though the Treatments hub they link to is still a 404 until S7 — expected, see earlier Carried Forward note on future-page links).

**Files created/changed:** `assets/css/pages.css` (new), `assets/js/main.js` (treatment-page generator block; `pageContext` derivation updated; `flags` added to the `content.js` import), `assets/js/content.js` (`signs`/`steps`/`aftercare`/`faqs`/`costRange` populated for 6 treatments), `treatments/dental-implants.html`, `treatments/gum-treatment.html`, `treatments/laser-gingivoplasty.html`, `treatments/laser-dentistry.html`, `treatments/root-canal.html`, `treatments/laser-rct.html` (all new).

### 2026-08-16 — S7 Remaining treatments + hub

Completed the treatments section of the site: the 5 remaining treatment pages, the treatments hub, and cross-linking.

**`content.js`:** populated `signs[]` (4 each), `steps[]` (5 each, `{title, desc}`), `aftercare[]` (4 each), `faqs[]` (4–5 each), and `costRange` (`{min, max, unit, emi}`) for the 5 remaining treatments — crowns-and-bridges, dental-restoration, braces-and-aligners, teeth-whitening, kids-dentistry — following the exact shape S6 established. Added a new top-level `alsoOffered` export (6 MOCK-flagged strings) for CONTENT-DATA.md §5's "also offered" list, which previously existed only as prose in that doc.

**5 HTML files** at `treatments/{crowns-and-bridges,dental-restoration,braces-and-aligners,teeth-whitening,kids-dentistry}.html`, generated from the S6 `root-canal.html` template (none of these 5 use the comparator, matching CONTENT-DATA §5). Each page's signs/aftercare section eyebrows were hand-tailored per treatment rather than left generic, matching the quality bar S6 set for the other pages.

**`treatments/index.html`** (new hub page): intro section, then a grid grouped into 5 pillars (Implants; Gum & periodontal care; Root canal; The laser method; General & family dentistry) covering all 11 treatments, then the "also offered" list, then the standard CTA band. The pillar grouping is hand-written page structure in `main.js` (no pillar field exists in `content.js`), but every card's content is still generated from `content.js` via a new shared `treatmentCardHtml()` helper — factored out of the homepage's existing treatments-grid generator so the same card markup isn't duplicated between the two grids.

**Related treatments:** added a new "Related treatments" section to the shared treatment page template, rendered from each treatment's `related[]` field (present in `content.js` since S1 but never rendered anywhere). This is SESSION-PLAN.md's S7-scoped "related-treatment links between pages" item — it touched all 11 treatment pages, including the 6 already shipped by S6, since the template is shared. Placed between the credential block and the CTA band.

**`assets/css/pages.css`:** added styles for the hub intro, hub pillar groups, also-offered list (reuses the existing `.chip` component for items), and the new related-treatments section.

QA'd via a Playwright agent across the hub, all 5 new treatment pages, and `dental-implants.html` (spot-check that the related-treatments addition didn't regress an existing S6 page) at 360×800/768×1024/1440×900: zero console errors, zero failed requests, zero horizontal overflow anywhere. Confirmed the hub grid renders exactly 11 cards across 5 groups, the also-offered list renders 6 items, every new page's signs/steps/aftercare/FAQ/related-treatments lists populate with the correct counts, cost blocks stay `hidden` (showPricing still false), credential blocks populate a performing dentist, and scroll-reveal correctly progresses (5→29 `.is-revealed` elements over an incremental scroll on the hub page, checked via DOM state rather than a `fullPage` screenshot per the known stitching-artifact note).

**Done-when check:** all 11 treatment pages exist ✅ · hub links to every one ✅ · hub mentions the also-offered list ✅.

**Files created/changed:** `assets/js/content.js` (`signs`/`steps`/`aftercare`/`faqs`/`costRange` populated for 5 treatments; `alsoOffered` export added), `assets/js/main.js` (`treatmentCardHtml()` helper factored out; treatments-hub generator added; also-offered list generator added; related-treatments generator added to the treatment-page template block; `alsoOffered` added to the `content.js` import), `assets/css/pages.css` (hub intro/group/also-offered/related-treatments styles added), `treatments/index.html` (new), `treatments/crowns-and-bridges.html`, `treatments/dental-restoration.html`, `treatments/braces-and-aligners.html`, `treatments/teeth-whitening.html`, `treatments/kids-dentistry.html` (all new), plus the 6 existing S6 treatment pages (`dental-implants.html`, `gum-treatment.html`, `laser-gingivoplasty.html`, `laser-dentistry.html`, `root-canal.html`, `laser-rct.html`) each gained the new related-treatments section.

### 2026-08-16 — S8 About, Team, Technology

Built the three remaining "about the practice" pages: `about.html`, `team.html` (+ 3 individual doctor pages), `technology.html`.

**`content.js`:** added two new top-level exports — `about` (`story`/`philosophy`, both `MOCK:`-flagged prose paragraphs) and `facility` (4 `MOCK:`-flagged facility-photo labels, 3:2 aspect per CONTENT-DATA §7). `doctors[]` and `equipment[]`/`sterilisation[]` (both from S5) were reused as-is, per the session's own instruction not to invent a second shape.

**`about.html`:** hero naming the clinic + founded year, a story section, a philosophy section, a facility photo grid (`#facilityGrid`, new generator), and the full sterilisation protocol reusing S5's `.sterilisation-list` checklist component on the same `sterilisation[]` array technology.html also reads (see Decision log). Closes with the standard CTA band (drape, this page's one dark section).

**`team.html`:** a full doctor grid (`#teamGrid`, new generator) reusing the homepage's `.doctor-grid`/`.doctor-card` markup and CSS exactly (principal variant for Dr. Sampath, standard cards for the 2 MOCK associates), plus a new "View profile" link per card gated on `flags.showDoctorPages` (currently `true`).

**Individual doctor pages** at `team/<slug>.html` (`dr-sampath-kumar-rao-k.html`, `MOCK-dr-anitha-rao.html`, `MOCK-dr-farah-khan.html`): generated from a one-off Python script (not shipped, deleted after running — same S6/S7 precedent), each carrying `data-doctor-slug` on `<body>`. A new `main.js` block (section 20) mirrors the treatment-page template pattern exactly — `activeDoctor` derived from the body attribute, `data-bind-doctor` attributes for name/degrees/reg/bio/photo-label, and a `#doctorTreatmentsList` generator linking out to each treatment the doctor performs (reusing S7's `.related-treatments__link` styling).

**`technology.html`:** the same `#equipmentGrid` generator the homepage already uses (reused unmodified — same ID, same data), plus a new `#sterilisationSteps` generator (section 21) presenting the same `sterilisation[]` strings as a numbered `.step-process` workflow instead of a checklist — reusing the step-process CSS component as-is (CONVENTIONS §5's one 01/02/03 numbering exception, already established by the treatment-page step process).

**Site-wide nav fix:** added a "Technology" link to the desktop nav, mobile overlay nav, and footer "Site" block on all 13 pre-existing pages (`index.html` + 11 treatment pages + the treatments hub) via a scripted insert, matching each file's existing path convention. Without this, `technology.html` had no inbound link from anywhere but the two other new pages. See Decision log for why this was judged in-scope.

Verified with a Playwright script across all 6 new pages at 360×900/768×900/1440×900: zero console errors, zero failed requests, zero horizontal overflow at every width. Confirmed `#facilityGrid` (4) and `#sterilisationList` (4) populate on About; `#teamGrid` (3) on Team; `#equipmentGrid` (4) and `#sterilisationSteps` (4) on Technology; each doctor page's `h1` binds the correct name and `#doctorTreatmentsList` populates the correct count (5 for Dr. Sampath, 3 each for the two associates). Re-verified `index.html` and `treatments/dental-implants.html` after the nav edit — still zero console errors/failed requests/overflow, and the new Technology link is present in all three expected locations.

**Done-when check:** three pages complete and responsive ✅ (360/768/1440 verified) · doctor data comes entirely from `content.js` ✅ (grepped — no doctor name/degree/bio/reg-no hardcoded into any of the 6 new HTML files, all populated via `data-bind-doctor`/generators).

**Files created/changed:** `assets/js/content.js` (`about`, `facility` exports added), `assets/js/main.js` (`about`/`facility` import; `foundedYear`/`aboutStory`/`aboutPhilosophy` added to bindings; `doctorSlug`/`activeDoctor` derived value added; sections 18–21 added: facility grid, team grid, individual doctor page template, sterilisation-as-step-process), `assets/css/pages.css` (about-hero/prose-section/facility-grid/team-hero/doctor-hero/doctor-treatments/technology-hero/sterilisation-workflow styles added), `about.html`, `team.html`, `technology.html` (all new), `team/dr-sampath-kumar-rao-k.html`, `team/MOCK-dr-anitha-rao.html`, `team/MOCK-dr-farah-khan.html` (all new), plus `index.html` and all 11 `treatments/*.html` files (Technology nav link added in 3 places each, no other changes).

### 2026-08-16 — S9 Patient info, Reviews, Contact, Privacy

Built the four remaining "practical" pages: `patient-information.html`, `reviews.html`, `contact.html` (with the four-field booking form), `privacy-policy.html`.

**`content.js`:** added a new top-level export, `patientInfo` (`firstVisit[]`, `whatToBring[]`, `payment[]`, `emiNote`, `insurance`, `emergency` — all `MOCK:`-flagged), following the S5/S8 precedent of adding a new export rather than a second data shape.

**`patient-information.html`:** hero, then five `.prose-section` blocks (reusing the About page's component as-is, S8) covering first visit, what to bring, timings (reuses `.visiting__hours`, populated via the existing generic `#visitingHours` generator in `main.js`), payment & EMI, insurance, and emergencies — closing with the standard CTA band.

**`reviews.html`:** intro hero, the homepage's `.rating-monument` markup reused verbatim per the session's own handoff note, then `.stories`/`#reviewsGrid` (also reused verbatim — both already fully generic components, no new CSS needed), closing CTA band.

**`contact.html`:** intro hero, a `.visiting` section (map placeholder, address, hours, directions/call CTAs — same component as the homepage's "Visiting the clinic"), and the new **booking form** — four fields only (name, phone, treatment interest, preferred day), consent checkbox + privacy-policy link directly above the submit button, real `<label>`s, inline validation. Treatment-interest `<option>`s are generated from `treatments[]` in `main.js` (section 23) so they can't drift from the treatments the clinic actually offers. No CTA band on this page — the booking form already is the page's one job.

**`assets/js/form.js`** (new file, completing the file list CONVENTIONS §2 named since S1): validates all four fields + consent on blur and on submit (name length, a 10-digit Indian mobile pattern for phone, non-empty select/date), sets `aria-invalid` and per-field error text, focuses the first invalid field on a failed submit attempt. On a valid submit it hides `#bookingForm` and reveals a static `#bookingConfirmation` panel reading "Consultation requested" (the CONTENT-DATA §6 same-name-through-the-flow rule) — **no real backend exists yet**; S11 owns wiring the actual form handler + WhatsApp notification path and should extend form.js's valid-submit branch rather than replace the validation logic.

**`privacy-policy.html`:** hero + six `.prose-section` blocks (what we collect, why, retention, your rights, contact) written as static DPDP-notice prose — not `content.js`-modeled, same reasoning as the comparator's row copy (S3) — with `email`/`phoneDisplay`/`name`/`locality`/`city` data-bound where the text needs a real fact.

QA'd via two Playwright agent passes at 360×800/768×1024/1440×900 across all four pages plus the booking form's full interaction flow (empty-submit validation, keyboard tab order, mouse-driven fill-and-submit). First pass caught two real bugs, both fixed and re-verified: (1) `.booking-form__error:empty { display: none; }` let the error slot's `min-height` collapse, shifting the consent checkbox mid-interaction and causing missed mouse clicks — removed the rule so the slot's height is constant; (2) `.booking-form` had no `[hidden]` rule, so `form.js` setting `form.hidden = true` didn't visually hide it (its own `display: flex` beat the UA default) — added `.booking-form[hidden] { display: none; }`. Final pass confirmed: zero console errors, zero failed requests, zero horizontal overflow on all four pages at all three widths; all data-bind/list/option generators populate with correct counts; empty-submit shows all 5 inline errors with no navigation; full keyboard tab order reaches every field, the consent checkbox, and submit; a valid mouse-driven submission hides the form and shows the "Consultation requested" confirmation.

**Done-when check:** four pages complete ✅ · form validates inline with real `<label>`s ✅ · privacy policy linked from every page footer ✅ (already true since S1's frame — verified still true on all 4 new pages).

**Files created/changed:** `assets/js/content.js` (`patientInfo` export added), `assets/js/main.js` (`patientInfo` import; `email`/`emiNote`/`insurance`/`emergency` added to bindings; sections 22–23 added: patient-info checklist generators, booking-form treatment-select options), `assets/js/form.js` (new), `assets/css/pages.css` (patient-hero/reviews-hero/contact-hero/booking-form/booking-confirmation/privacy-hero styles added), `patient-information.html`, `reviews.html`, `contact.html`, `privacy-policy.html` (all new).

### 2026-08-16 — S10 Blog

Built the blog section: `blog/index.html` (card grid), a reusable post-page shell, and 5 seed posts.

**`content.js`:** added a new top-level export, `blogPosts` (5 fully-`MOCK:`-flagged posts — CONTENT-DATA §3 lists blog posts as fully invented). Each post carries `slug`/`title`/`category`/`publishedDate`/`readTime`/`coverLabel`/`excerpt` plus `body: [{id, heading, paragraphs[]}]` — one array that drives the rendered post content, the rail's table of contents, and the TOC's anchor targets, so they can't drift apart. Topics: bleeding gums (gum care), root canal step-by-step (root canal), implants vs. bridges (implants), a child's first visit (kids dentistry), and what laser dentistry's light actually does (laser dentistry/method framing) — all written to the same does-vs-gets DCI test as every other page (CONVENTIONS §1), with the suitability disclaimer worked into the procedure-specific posts.

**`blog/index.html`:** hero + a `#blogGrid` card grid (16:9 cover placeholder, category chip, title, excerpt, date + read time), standard `.rail__label` rail (this page needs section tracking, not a TOC), standard CTA band.

**Post template (5 HTML files, one per post):** hero (category chip + date/read-time meta, `h1`, 16:9 cover placeholder) → `#postBody` (68ch `--measure` prose, generated from `body[]`) → CTA band. Authored once as a shell and copied per file (not a disposable generator script this time — each post's prose is bespoke, not a handful of interpolated fields, so there was nothing repetitive enough to script; see Decision log). `<title>`/`<meta description>` hand-written and clean per file, same S6 precedent.

**The signature piece: `.rail__toc`.** Per DESIGN-SYSTEM §7's explicit call for a sticky table of contents in the rail on desktop, post pages swap the standard rail-label/scroll-track for `<nav class="rail__toc">` — one anchor link per `body[]` section — tracked by a second, independent `IntersectionObserver` in `main.js` (section 25, `rootMargin: "-20% 0px -70% 0px"`) that toggles `.is-active` on whichever link's heading is in the reading zone. Scoped to post pages only (only instantiated when `#postToc` exists in the DOM); doesn't interact with the existing section-6 rail-label tracker or motion.js's scroll-reveal observer. `.rail__toc`/`.rail__toc-link` were added to `components.css` beside `.rail__label`/`.rail__track` (a rail variant, not page content) rather than `pages.css`. No extra mobile-hiding code was needed — `.rail` is already `display: none` below 1024px, so the TOC mode inherits that for free.

**`main.js`:** imported `blogPosts`; added `postSlug`/`activePost` derived values (section 1, same pattern as `treatmentSlug`/`doctorSlug`) and folded `activePost.title` into the WhatsApp `pageContext` fallback chain; section 24 (blog index grid generator) and section 25 (post-page template — body generator + TOC generator + the TOC's scroll-spy observer).

**CSS:** `pages.css` gained `.blog-hero`/`.blog-index`/`.blog-grid`/`.blog-card` and `.post-hero`/`.post`/`.post__section`/`.post__heading`/`.post__paragraph`; `components.css` gained `.rail__toc`/`.rail__toc-link` inside the existing `≥1024px` rail media block.

QA'd via a Playwright agent across all 6 blog pages at 360×800/768×1024/1440×900: zero console errors, zero failed requests, zero horizontal overflow anywhere; `#blogGrid` renders exactly 5 cards with populated title/excerpt/date/category, and clicking one navigates to a working post page; a spot-checked post's `#postBody` renders exactly 4 `.post__section` blocks matching its `content.js` data; the rail TOC renders 4 links matching the section headings and genuinely tracks scroll position (verified via incremental scroll, not a single jump — `.is-active` moved through all 4 links in order, with brief dual-active states during transitions between adjacent sections); clicking a TOC link jumps to the right anchor; the rail (and therefore the TOC) is confirmed hidden (`display: none`) at 360px. Re-verified `index.html` and `treatments/dental-implants.html` still load clean after the shared `main.js` edit.

**Done-when check:** index + 5 posts render ✅ · TOC tracks scroll on desktop ✅ (verified via incremental scroll, not just DOM presence) · TOC hidden on mobile ✅ (inherits `.rail`'s existing `display: none` below 1024px).

**Files created/changed:** `assets/js/content.js` (`blogPosts` export added), `assets/js/main.js` (`blogPosts` import; `postSlug`/`activePost` derived values; `pageContext` fallback updated; sections 24–25 added), `assets/css/pages.css` (blog-hero/blog-index/blog-grid/blog-card/post-hero/post styles added), `assets/css/components.css` (`.rail__toc`/`.rail__toc-link` added), `blog/index.html` (new), `blog/bleeding-gums-what-it-means.html`, `blog/root-canal-what-actually-happens.html`, `blog/dental-implants-vs-bridges.html`, `blog/childs-first-dental-visit.html`, `blog/what-laser-dentistry-actually-does.html` (all new).

### 2026-08-16 — S11 Technical layer

Built the site-wide technical/SEO/analytics layer across all 29 real pages.

**Schema (`assets/js/schema.js`, new):** injects JSON-LD at runtime rather than as static per-page `<script>` blocks, so every field stays sourced from `content.js` or from DOM text `main.js` has already bound — no second hardcoded copy of client facts (CONVENTIONS §3). Every page gets a `Dentist` entity (address, phone, aggregate rating, opening hours from `clinic.hours`). Every inner page's existing `.breadcrumb__list` markup (built by S6–S10, previously visual-only) is read back out into a `BreadcrumbList`. Any page with `.faq-item` elements (homepage accordion or a treatment page's) gets `FAQPage`. Treatment pages get `MedicalProcedure` (name/description/howPerformed/followup/relevantSpecialty, deliberately no `procedureType`/`bodyLocation` guess since 11 different procedures can't be correctly classified with one fixed value). Doctor pages get `Physician`. Blog posts get `Article`. Schema text is never cleaner than the visible page — `MOCK:` prefixes carry through on purpose, see Carried forward.

**Meta tags:** a one-off script (`gen_meta_tags.py`, deleted after running, per the S6+ generator-script precedent) added `<link rel="canonical">` + full OG/Twitter tags to all 29 pages, derived from each page's already-existing `<title>`/`<meta description>` (`index.html` was missing a description entirely — added one). A second one-off script (`gen_script_tags.py`) added the `analytics.js`/`schema.js` `<script>` tags after `motion.js` on every page.

**GA4 (`assets/js/analytics.js`, new):** loads `gtag.js` and fires all five named conversion events (`whatsapp_click`, `call_click`, `directions_click` via the existing `[data-cta]` elements; `comparator_toggle` via a new `comparator:toggle` `CustomEvent` `comparator.js` now dispatches on a real toggle; `form_submit` via a direct import into `form.js`) — but only when `content.js`'s new `analytics.ga4MeasurementId` is a real `G-` ID, not the `MOCK:` placeholder, so the build makes zero analytics network calls until the client provides one.

**Booking form handler (`form.js` rewritten, `contact.html` form markup extended):** wired to Netlify Forms — `data-netlify="true"`/honeypot/hidden `form-name` static markup plus an async `fetch("/", ...)` POST on valid submit. A failed submission now shows a real inline error (`#bookingSubmitError`) instead of silently succeeding; a successful one populates a new WhatsApp-prefill button on the confirmation panel (`#bookingConfirmationWhatsapp`) with the submitted name/phone/treatment/day, as the "notification path" SESSION-PLAN.md asked for — see Carried forward for why this is client-side, not a server webhook.

**Site plumbing (all new):** `favicon.svg` (a generated mark reusing the existing treatment-card beam motif, not the client's real logo) + rasterized `.ico`/16/32/180/192/512px PNGs (ImageMagick, palette-reduced) + `site.webmanifest`, linked from every page; `robots.txt`; `sitemap.xml` (29 URLs, generated from the same file list the meta-tag script used).

QA'd via three Playwright passes covering all 29 pages (a representative sample per page-type first, confirming JSON-LD `@type`s and zero console errors/failed requests; then the remaining pages in bulk; then a targeted booking-form flow test against both a POST-rejecting and a POST-accepting local server to verify the error path and the success path — including the WhatsApp link's populated `href` — both work correctly). Caught and fixed one real bug in the process: the breadcrumb-derived `BreadcrumbList`'s "Home" item resolved to `.../index.html` instead of `.../` (an artifact of every page's nav literally linking to `/index.html`) — `schema.js` now strips a trailing `index.html` when building breadcrumb item URLs.

**Done-when check:** every page validates in Google's Rich Results Test — not run against the live tool this session (no deployed URL to test yet, Open question #12), but all six schema types were verified structurally valid and correctly scoped via local JSON-LD inspection ✅ · all five GA4 events fire — verified wired correctly, but inert until a real measurement ID exists (by design) ⚠️ · a test form submission arrives — verified the Netlify Forms POST contract locally against a mock server; an actual arrival can only be confirmed once the site is deployed on Netlify ⚠️.

**Files created/changed:** `assets/js/content.js` (`clinic.siteUrl`, `analytics` export added), `assets/js/schema.js` (new), `assets/js/analytics.js` (new), `assets/js/comparator.js` (dispatches `comparator:toggle`), `assets/js/form.js` (rewritten: async Netlify submit, WhatsApp-prefill confirmation, `trackEvent` import), `assets/css/pages.css` (`.booking-confirmation .btn`), `contact.html` (Netlify form attributes, honeypot, `#bookingSubmitError`, `#bookingConfirmationWhatsapp`), `favicon.svg`, `favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png`, `assets/img/icon-192.png`, `assets/img/icon-512.png`, `site.webmanifest`, `robots.txt`, `sitemap.xml` (all new), plus canonical/OG/Twitter meta tags + favicon links + `analytics.js`/`schema.js` script tags added to all 29 existing HTML pages.

### 2026-08-17 — S12 QA & polish

Final session of the 12-session plan. Full-site verification pass across all 29 real pages (the `_kitchen-sink.html` scratch page is now deleted, per SESSION-PLAN) using a headless-Chromium Playwright harness — not just source-reading, per CONVENTIONS' instruction to verify real rendered behaviour.

**Swept and clean, no fix needed:** zero console errors and zero failed network requests on all 29 pages; zero horizontal overflow at 360×640, 390×844, 768×1024, 1024×800, and 1440×900 on all 29 pages; zero dead internal links (resolved every non-external `href` against the filesystem); zero missing `alt` attributes (there are no `<img>` tags anywhere yet — see Carried forward on why image-conversion scope was a no-op); WCAG AA contrast computed and passing on every fg/bg token pair in the system (worst case 4.90:1, `--beam-deep` accent text on `--bisque`); no `outline: none` anywhere; LCP 1.3–1.8s on all sampled pages under a throttled-network + 4×-CPU-throttle CDP profile, well inside the 2.5s budget; reduced-motion ambient glow/scroll-reveal/comparator-swap behaviour all correctly gated already (verified via `page.emulateMedia`), needing no changes.

**Found and fixed, six real bugs:**
1. Heading-order skips (`H1` straight to `H3`, no `H2` in between) on `team.html`, `treatments/index.html`, and `blog/index.html` — each page's card-grid section had no heading of its own. Fixed with a `visually-hidden` `<h2>` in each grid section (no visual change, just correct document structure).
2. `.mobile-menu` had no `[hidden] { display: none; }` rule, so the native `hidden` attribute did nothing against the component's own `display: flex` — the closed off-canvas menu was fully laid out and in the tab order on every page, site-wide, since S2. Confirmed via a real keyboard-tab trace: logo → hamburger → the invisible menu's close button. Fixed with the missing CSS rule; `main.js`'s existing attribute-toggle logic needed no change.
3. The rating monument's SVG arc had no `stroke-dasharray`/`dashoffset` under `prefers-reduced-motion: reduce`, so it rendered as a full undashed circle (visually a false 5.0 rating) instead of the correct ~98% (4.9-of-5) arc. Fixed with a dedicated reduced-motion rule setting the final dashed state directly, no transition.
4. `.booking-form__error`'s submit-slot placement on `contact.html` (flagged unreviewed by S11) — the empty error paragraph reserved a dead ~64px gap between the consent checkbox and the submit button (2.5× the form's normal ~24px rhythm) because it was a direct flex child of `.booking-form`. Fixed by grouping it with the button in a new `.booking-form__submit` wrapper with a tighter 8px inner gap.
5. Comparator toggle buttons (`min-height: 40px`) and footer social icons (`36px` square) were both below the CONVENTIONS §7 48×48px tap-target floor, and both were raw-px-literal violations of the tokens-only rule. Fixed by switching both to `var(--space-12)` (48px).
6. Verified, not a bug: favicon contrast at 16×16 (flagged unreviewed by S11) — rendered the actual PNG at native size against both light and dark synthetic tab backgrounds, computed `--beam`/`--drape` contrast at 6.43:1, well past AA. No change needed.

**Deliberately not fixed, flagged instead:** the footer treatment-links list and inline contact text-links (phone/privacy-policy) sit well under 48×48px — a site-wide pattern since S2, not a single-page bug, and a proper fix means a footer-density redesign decision out of a QA session's scope (see Carried forward). Cross-browser testing was Chromium-only — Firefox and WebKit aren't installed in this sandbox for Playwright; flagged for a future spot-check once real hosting exists, particularly the `<details>`/`<summary>` FAQ accordion and the `grid-template-rows` trick.

**Done-when check (SESSION-PLAN's own bar was "Lighthouse ≥90 on all four categories" — Lighthouse itself wasn't run, no CLI available in this sandbox; verified the underlying signals directly instead):** performance — LCP <2.5s confirmed under throttled conditions ✅, image scope confirmed a no-op (no images exist yet) ✅; accessibility — contrast/heading-order/landmarks/focus-rings/keyboard-paths/alt-text all verified, 3 real bugs found and fixed ✅; responsive — zero overflow at all 5 target widths across all 29 pages ✅; reduced-motion — verified correct across ambient/reveal/comparator, 1 real bug found and fixed (rating arc) ✅; dead links — zero found ✅; console errors — zero found ✅; `_kitchen-sink.html` deleted ✅.

**Files created/changed:** `team.html`, `treatments/index.html`, `blog/index.html` (each gained a `visually-hidden` `<h2>` in one section), `contact.html` (`.booking-form__submit` wrapper added around the submit error + button), `assets/css/components.css` (`.mobile-menu[hidden]` rule added; `.site-footer__social a` resized to `var(--space-12)`), `assets/css/sections.css` (`.rating-monument__arc-fill` reduced-motion block added; `.comparator__toggle-btn` resized to `var(--space-12)`), `assets/css/pages.css` (`.booking-form__submit` rule added). **Deleted:** `_kitchen-sink.html`.

**This was the last of the 12 planned sessions.** The site is build-complete on mock data. See "Next session" at the top of this file and SESSION-PLAN.md's "After S12" section for what remains — all of it client-dependent.
